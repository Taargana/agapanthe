# Agapanthe — contexte projet (à lire en premier)

Moteur de jeu **Vulkan en C# from scratch** (.NET 10), cross-platform (Windows / Linux / macOS).
**Phase 1 (TERMINÉE, 8/8)** : chaîne graphique 3D. **Phase 2 (TERMINÉE, 5/5)** : fondations scalables — ECS (Arch), `double` + camera-relative à origine quantifiée, culling, montée en charge (10k entités à 10 000 km, NativeAOT). **Phase 3 (en cours)** : gameplay — **P3-M1 instancing** ✅, **P3-M2 scheduler + lifecycle + `Agapanthe.Engine`** ✅, **P3-M3 physique v1** ✅, **P3-M4 rendu GPU-driven** ✅, **P3-M5 CSM** ✅, **P3-M6 slots persistants + cull d'ombre GPU** ✅, **P3-M7 device-local + raster ombre 4×** ✅, **P3-M8 premier pas planétaire (reversed-Z + scène planète/Soleil 1/2)** ✅, **VS-1 sérialisation du World (save/load snapshot)** ✅, **VS-2 spawn runtime différé + gravité newtonienne** ✅, **VS-3 glu gameplay (défi d'atterrissage planétaire)** ✅, **UI-1 texte à l'écran** ✅, **UI-2 overlay debug + profiler CPU** ✅, **MP-0a headless split** ✅, **MP-0b identité d'entité** ✅, **MP-0c autorité du temps (fixed-timestep accumulator)** ✅ ; cap : **« vrai engine »** (backlog §4quater — VS-4/VS-5 en pause) → **MP-0d (input → commandes horodatées)**, ou **UI-3 (timestamps GPU)** ; validation Linux/macOS (P3-M0) toujours dûe.

## Où est la vérité (lis ces fichiers avant d'agir)

- **`docs/AVANCEMENT.md`** — état d'avancement, jalons, métriques, plan du prochain jalon, **section « Reprise — où repartir »**. C'est le point d'entrée pour reprendre le travail.
- **`docs/BACKLOG.md`** — ce qu'on sait devoir faire *plus tard*, avec le pourquoi et **quand ça mord** (CSM, rendu GPU-driven, nuages volumétriques, atmosphère, ombres analytiques planétaires, physique…). À lire avant de choisir un jalon ; à mettre à jour quand une décision technique est instruite.
- **`docs/plans/2026-07-02-graphics-engine-design.md`** — spec de référence de la phase graphique (§ numérotés, cités partout).
- **`.absolute-human/board.md`** — board de la session courante ; sessions passées dans `.absolute-human/archive/board-sessionN-*.md`.
- **`docs/visual-checks/`** — protocoles de validation visuelle par jalon (verdicts humains).

> La mémoire persistante de Claude (`~/.claude/…/memory/`) **ne voyage pas avec le dépôt**. Sur un nouvel appareil, tout le contexte nécessaire est dans les docs ci-dessus — commence par `docs/AVANCEMENT.md`.

## Décisions structurantes (verrouillées — ne pas re-litiger)

- Bindings **Silk.NET** (Vulkan + GLFW + input) ; le reste from scratch.
- Baseline **Vulkan 1.2 + dynamic_rendering + synchronization2** (chemin 1.3 core sur MoltenVK).
- Maths **System.Numerics** (convention row-vector) + helpers clip-space Vulkan (Y-flip, Z [0,1]).
- Shaders **GLSL → SPIR-V à l'exécution (shaderc)**, cache disque.
- Couche GPU **mince mono-backend** : **aucun type `Vk*` ne sort de `Agapanthe.Graphics`**.
- Allocateur GPU from scratch ; **IDisposable partout**, destruction différée N+2 frames (DeletionQueue non-capturante), **zéro alloc managée par frame** sur le hot path, **ResourceTracker (tout leak = échec du run)**.
- **Tout message de validation layer = bug.** `.NET 10`, `TreatWarningsAsErrors`.
- MoltenVK portability : pas de comparateur mutable, pas d'imageCubeArray (vérifier chaque feature au premier VUID).

## Modules

```
Core ◄── World ◄── Engine                       LA SIMULATION — headless, closure = {Core, World}
                     ▲                          (aucun Rendering / Graphics / Platform / Silk.NET :
                     │                           EngineIsHeadlessTests l'impose, 6 cas)
Core ◄── Graphics ◄── Rendering ◄── Engine.Render   la moitié rendu (compose un SimulationHost)
                                        ▲
                          Sandbox (fenêtré)  ·  HeadlessSim (serveur, NativeAOT, sans GPU)
Rendering ──► Ui ──► Assets ──► Core            (Ui et Assets GPU-free)
Platform ──► Core                               (fenêtre GLFW, input — jamais référencé par Engine)
```
`Agapanthe.Graphics` reste le **seul** projet référençant Silk.NET.Vulkan ; aucun type `Vk*` n'en sort.

## Commandes

```sh
# prérequis macOS : brew install molten-vk vulkan-loader vulkan-validationlayers vulkan-tools
dotnet build
dotnet test
# Sandbox (le loader Vulkan Homebrew est hors des chemins GLFW par défaut → DYLD_LIBRARY_PATH)
DYLD_LIBRARY_PATH=/opt/homebrew/lib dotnet run --project samples/Sandbox
DYLD_LIBRARY_PATH=/opt/homebrew/lib dotnet run --project samples/Sandbox -- MetalRoughSpheres.glb
```

Env vars debug : `AGAPANTHE_MAX_FRAMES=N` (auto-close, sortie 0 si 0 leak) · `AGAPANTHE_CAPTURE=out.ppm` (dump HDR tonemappé) · `AGAPANTHE_VIEW="x,y,z"` (angle caméra) · `AGAPANTHE_HDRI=<path.hdr>` (environnement IBL) · `AGAPANTHE_IBL_TEST=<préfixe>` (génère l'IBL headless + dump faces/maps) · `AGAPANTHE_SHADER_RELOAD_TEST=1` (force un reload des 4 passes + logge le wall-time).

> **MP-0c — déterminisme des captures** : depuis le fixed-timestep accumulator, un run déterministe (capture, banc)
> **DOIT** poser `AGAPANTHE_MAX_FRAMES` (> 0). Ce flag seul bascule le Sandbox sur un dt synthétique constant (= le
> pas fixe, 1 tick/frame) ; sans lui l'accumulateur consomme le vrai dt wall-clock GLFW et le run n'est plus
> reproductible run-à-run. `AGAPANTHE_CAPTURE`/`AGAPANTHE_CULL_STATS` seuls ne suffisent pas.

**Hot reload shaders** (M8) : éditer un fichier de `shaders/` pendant que le Sandbox tourne → recompile + recréation du pipeline en < 1 s. Échec de compile = log + ancien pipeline conservé. Limites : éditions **interface-compatibles** uniquement (changer un binding → restart) ; les 4 shaders compute IBL ne sont pas surveillés.

## Méthode de travail

Sessions pilotées via **absolute-human** (décomposition en tâches, vagues parallèles, TDD). Chaque jalon clôt sur : Sandbox propre (0 message validation, 0 leak), tests verts, **double audit agent** (`csharp-lowlevel` + `engine-architect`) PASS, protocole visuel humain quand pertinent, board archivé.

## Préférences de travail (l'humain)

- **Commits sur demande uniquement** : ne jamais `commit`/`push` sans demande explicite. L'humain pilote le rythme et valide chaque étape.
- **Travail en vagues + feu vert** : décomposer en vagues, attendre l'accord de l'humain entre les étapes, lancer les audits subagents (`csharp-lowlevel` + `engine-architect`) avant de clore un jalon.
- **Vérification visuelle headless** : captures headless (`AGAPANTHE_CAPTURE`) à chaque vague pour prouver le rendu ; **verdict visuel humain** requis avant de marquer un jalon `done`.
- **Gate 0 leak / 0 validation** : tout message de validation layer ou tout leak ResourceTracker est **bloquant**, jamais ignoré ni contourné.
- **Langue** : conversation en **français** (phrases complètes) ; **code, commits, PRs et docs techniques en anglais**. Dans la prose française, les **termes techniques du domaine restent en anglais** (shader baking, culling, frustum, shadow mapping, tie-break, radix sort, lockstep, instance buffer, server meshing…) — ne pas les traduire (« précuit » pour *baking* est à proscrire).

## État courant

**PHASE 1 CLOSE (8/8). PHASE 2 CLOSE (5/5) — P2-M0 → P2-M4 clos** (gate AOT + Arch · SPIR-V hors-ligne · couture ECS · camera-relative · **frustum culling + montée en charge**). Métriques : 275 tests, 0 warning, 0 message de validation, 0 leak, probe NativeAOT PASS. Double audit signe la clôture de la phase.

**P2-M4 (session 13)** : le monde stocke les positions en `double` à **origine quantifiée** (snap 1024 m), ne dessine que ce que voit la caméra (frustum culling), et le prouve à l'échelle — **10 000 entités cullées à 10 000 km, 0 alloc/frame, en NativeAOT**, image bit-identique à l'origine (maille alignée). Env vars : `AGAPANTHE_WORLD_ORIGIN="x,y,z"` · `AGAPANTHE_SCENE=grid:NxN` (banc) · `AGAPANTHE_CULL_STATS=1` (stats) · `AGAPANTHE_UNLOAD_TEST=N`.

**UI-1 (session 25, clos) — texte à l'écran** : premier des 3 jalons Texte & UI (spec `docs/plans/2026-08-03-text-ui-design.md`, approuvée 4,4/5 après 2 tours de revue scorée). **Rasterisation SDF HORS-LIGNE** : `tools/FontCooker` (console non-AOT, jamais référencée par un projet shippé) utilise **`StbTrueTypeSharp`** (pur managé, sœur de `StbImageSharp`) → **zéro dépendance native, y compris dans l'outil** ; FreeType écarté (n'apporte que du hinting, inutile en SDF). Format **`.agfont`** binaire déterministe (patron VS-1) lu par `Agapanthe.Assets`. Nouveau projet **`Agapanthe.Ui` GPU-free** (`UiDrawList`, shaping avec seam `Rune` pour HarfBuzz plus tard, layout, `Measure`) ; **`Rendering` référence `Ui`** (forcé : `StorageBufferRing<T>` est `internal` à Rendering). Rendu : **1 pipeline / 1 atlas / 1 draw call**, SSBO + `gl_VertexIndex` sans vertex buffer, un **set per-frame**, après le tonemap en `LoadOp=Load`, blend **premultiplied** (nouveau `BlendMode` — le blending était câblé en dur à `false`). Ordre shader obligatoire : sRGB→linéaire **puis** ×alpha (l'inverse = halo). Atlas `R8Unorm`, **`MipLevels=1`** (les mips lisseraient le champ de distance). **Capture swapchain** ajoutée (`AGAPANTHE_CAPTURE_UI`) : `AGAPANTHE_CAPTURE` lit la cible HDR et ne peut donc **pas** voir un overlay dessiné après le tonemap. 435 tests, 0 warning, 0 validation, 0 leak, AOT PASS (`StbTrueTypeSharp` absent du publish), hashes `12638edd` (non-régression blending, inchangé) / `6e14b23e` (capture UI). Double audit PASS-with-concerns ×2 + verdict humain PASS. Env vars : `AGAPANTHE_CAPTURE_UI=<path.ppm>`.

**UI-2 (session 25, clos) — overlay debug in-view + profiler CPU** : deuxième des 3 jalons Texte & UI. `FrameStats`/`FrameSeries` (rings circulaires frame time + octets alloués, agrégats sans alloc) et `DebugOverlaySystem` (`ISystem` en `PostSimulation`, bascule **`F3`**, `AGAPANTHE_OVERLAY=0` pour démarrer masqué) dans `Engine` ; `Sparkline` (primitif de graphe GPU-free) et `TextBuilder` (formatage 0-alloc `TryFormat`+`stackalloc`, **public**) dans `Ui`. L'overlay **remplace le HUD `window.Title`** et son hack de cession VS-3 (`&& landingChallenge is null`) — `Program.cs` perd 79 lignes. **Le gate 0-alloc devient visible en continu à l'écran** (`alloc 0 B/frame` en vert, rouge dès qu'une frame alloue). **La leçon du jalon est la fenêtre de mesure, pas le compteur** : l'overlay a d'abord rapporté 272 B/frame là où le banc disait 0 (sa fenêtre englobait le pump d'événements GLFW, hors contrôle du moteur), puis la correction fermait le bracket **dans** `RecordCommandBuffer` — avant submit/present — et **jamais** sur les frames à sortie précoce (resize), affichant « 0 B » en vert pendant qu'une swapchain était recréée par frame. Le bracket vit désormais dans `FrameOrchestrator` (`Tick` ouvre, **`EndFrame()` après `DrawFrame`** ferme), exactement celui du banc ; `FrameStats.Record(float, long)` **reçoit** les mesures au lieu de les échantillonner. `GC.GetAllocatedBytesForCurrentThread()` est **par thread** → `Debug.Assert` d'ancrage mono-thread (MP-0 est le jalon qui casserait ça). Échelles de graphe calées sur la **frame courante**, pas sur le `Max` de la fenêtre (un pic de warm-up à 400 Kio rendait une régression de 512 B/frame invisible 4 s). Une **ligne = un `DrawText`** (plafond 256 glyphes). Un profiler affichant des timings réels ne peut pas être byte-identique → le gate déterministe porte sur l'**overlay masqué** (`03421357`), l'overlay lui-même sur verdict humain (diff inter-runs 432 px / 921 600, confiné au panneau). 468 tests, 0 warning, 0 validation, **0 hazard**, 0 leak, AOT PASS. Double audit PASS-with-concerns ×2 (aucun 🔴). **UI-3 (timestamps GPU) reste à faire.** Env var : `AGAPANTHE_OVERLAY=0`.

**Synchronization validation (session 25)** : activée par défaut en Debug (`VK_EXT_validation_features` — fournie par la COUCHE, donc invisible à une énumération d'extensions sans nom de couche), désactivable par `AGAPANTHE_SYNC_VALIDATION=0`. Elle a immédiatement révélé **3 hazards préexistants dans la boucle de frame**, invisibles depuis le début du projet : (1) la transition d'acquisition au stage `TOP_OF_PIPE` s'exécutait **avant** l'attente du sémaphore `imageAvailable` (posé à `COLOR_ATTACHMENT_OUTPUT`) → `WRITE_AFTER_READ` contre `vkAcquireNextImageKHR` ; (2) `renderFinished` signalé à `COLOR_ATTACHMENT_OUTPUT` alors que la transition vers `PresentSrc` s'achève à `BOTTOM_OF_PIPE` → `PRESENT_AFTER_WRITE` ; (3) `Undefined` comme source ne posait que le **stage**, pas l'**access** → dépendance d'exécution sans dépendance mémoire, `WRITE_AFTER_WRITE` sur le depth partagé (l'intention était dans le commentaire du code, la barrière était à moitié construite). Corrigés ; **captures `12638edd` et `6e14b23e` inchangées** (la correction ne change aucun pixel). **Le gate « 0 message de validation » couvre désormais les hazards de synchro.**

**MP-0a (session 26, clos) — headless split : la simulation tourne sans GPU** : premier des **4 sous-jalons** de MP-0, qui a été **DÉCOMPOSÉ** (les 4 points du backlog ne partagent ni fichiers ni risques ; un double audit sur 4 sous-systèmes hétérogènes ne vaut rien). Spec `docs/plans/2026-08-13-mp0a-headless-split-design.md`, approuvée **4,15/5** après 2 tours (v1 à 2,85 « Major Gaps » : elle avait bâti son gate central sur un **commentaire** au lieu du code exécutable — `CollectRenderLists` fait un `Set`, pas un `Add`, donc aucun churn d'archétype). **L'ordre exécuté inverse la numérotation du backlog**, qui classe par *sévérité* : le coût du split est **strictement croissant** avec la taille d'`Engine`, alors que les deux 🔴 identité ne sont pas cassés aujourd'hui et ne le deviennent **qu'au moment du partitionnement** (bug et correctif arrivent ensemble). **`Agapanthe.Engine` = simulation headless** (`Stage`, `TickContext`, `ISystem`, `SystemScheduler`, **`SimulationHost`**, `FrameStats`, `PhysicsSystem`, `LandingChallengeRule`) ; **`Agapanthe.Engine.Render`** prend le rendu (`RenderContext`, `IRenderSystem`, `RenderSystemScheduler`, `FrameOrchestrator`, `UiRenderSystem`, `DebugOverlaySystem`). **Le découpage suit les DÉPENDANCES, pas les interfaces** (`DebugOverlaySystem` est un `ISystem` et part côté rendu ; `UiRenderSystem` implémente les deux). `FrameOrchestrator` **compose** un `SimulationHost` et le **reçoit** en surcharge (une simulation existe ; un client y attache une présentation, jamais l'inverse). `RenderSystemScheduler` prend la barrière **directement** → aucun `InternalsVisibleTo` entre les deux assemblies. **`SimulationHost.BeginFrame()` est séparé de `Tick()`** : sous l'accumulator de MP-0c, N ticks partagent une frame et le bracket ouvert dans `Tick` aurait filé **N échantillons par frame** — le gate 0-alloc affiché en continu aurait cessé de mesurer une frame. **`AggregateBoundsSystem` + `_sceneBounds` supprimés** : écrits chaque frame, lus par personne depuis P3-M5 ; un commentaire périmé les a fait survivre 11 sessions. **`samples/HeadlessSim`** (NativeAOT, 1,68 Mo, exit 0, 0 B/frame, snapshot **JIT == AOT** `7c889fec`) est l'artefact de tête. **6 gates d'architecture** : allowlist **statique** sur le `.csproj` (Engine/World/HeadlessSim), « aucun `PackageReference` », closure d'assemblies, surface publique. **Le statique et la closure ne sont PAS redondants, c'est prouvé par mutation** : ré-ajouter `ProjectReference Rendering` sans utiliser de type → statique FAIL, closure VERT (le compilateur élide la référence inutilisée). 491 tests, 0 warning, captures `12638edd`/`03421357` inchangées, 0 leak, 0 validation. Double audit PASS-with-concerns ×2 (4,2/5), aucun 🔴. **Restent MP-0c/d.**

**MP-0b (session 27, clos) — identité d'entité : `GlobalId` 64 bits partitionné + clé de contact physique corrigée** : deuxième des 4 sous-jalons de MP-0. Spec `docs/plans/2026-08-13-mp0b-entity-identity-design.md`, approuvée **4,4/5** après 2 tours. **4 vagues, chacune gardant les captures `12638edd`/`03421357` inchangées jusqu'à la fin** : **W1** `ContactPairKey` (struct 128 bits comparable, `IComparable<T>` contraint jamais boxé) remplace le packing 32 bits `(gid_j<<32)|(uint)gid_k` qui collisionnait silencieusement dès que les ids cessaient d'être denses. **W2** `GlobalIdRange` (`[Start, EndExclusive)`, défaut `[1, ulong.MaxValue)` bit-pour-bit l'ancien compteur) — les 7 sites `_nextGlobalId++` collapsent dans un `NextId()` qui lève bruyamment à l'épuisement. **W3** `UniverseId` (identité **par snapshot**, deux `ulong` — pas un `Guid`, mixed-endian — défaut vide et **non aléatoire** pour préserver le déterminisme JIT-vs-AOT) + snapshot **v2** (en-tête 40 octets, v1 refusé, réconciliation d'univers à **5 cas** testés : both `None` inchangé · adopte · garde · confirme · **throw** sur conflit). **W4 — double audit (`csharp-lowlevel` + `engine-architect`) a trouvé et fait corriger un 🔴** : `Load` avec la 1ʳᵉ version de son paramètre d'allocateur (`GlobalIdRange?`) pouvait laisser un monde recouvrir des ids déjà chargés — les trois points de matérialisation écrivaient `_live[globalId] = entity` par **indexeur**, un spawn ultérieur réémettant le même id **écrasait silencieusement** l'entrée (entité chargée toujours simulée mais indespawnable, disparue du `Save` suivant). Corrigé par un `RegisterLive` unique (`TryAdd` + exception nommée). L'audit architecture a aussi fait remplacer `allocatorOverride: GlobalIdRange?` (qui laissait un bail se réassigner par un paramètre de désérialisation) par **`SnapshotAllocatorPolicy`** (`AdoptFromHeader`/`KeepMine`, sans donnée — le bail reste un fait fixé à la construction) ; `Load` retourne désormais **`SnapshotLoadResult`** au lieu de `void` (la mitigation « adoption loggée » promise par la spec, tenue sans dépendance de logging dans `Agapanthe.World` — l'hôte décide). **`HeadlessSim` re-épinglé** : `7c889fec…` (v1, 1852 o) → **`7e8dc68f5a25914c84677a7a53ad3a58`** (v2, 1868 o, delta = les 16 octets d'`UniverseId`), vérifié JIT==NativeAOT avant et après les corrections W4 ; **le gate vit désormais dans un test** (`HeadlessSimSnapshotFormatTests.cs`) et non plus seulement dans la prose — exactement le trou que la redécouverte de `7c889fec` en session 26 avait signalé. 530 tests, 0 warning, captures inchangées (vérifiées à chaque vague), 0 leak, 0 validation. Double audit PASS-with-concerns (architecture 4,1/5) / 🔴 trouvé-et-corrigé (bas niveau), verdict humain PASS. **Dette laissée** : 🟠 aucun hôte de production ne stampe encore d'`UniverseId` (Sandbox/HeadlessSim chargent en `UniverseId.None` — le **mécanisme** du 🔴 « deux mondes inmergeables » est fermé, son **usage** attend le premier hôte réel `Agapanthe.App`/MP-0c-d) · 🟡 pas de renouvellement de bail avant épuisement de `GlobalIdRange` (additif, aucune refonte requise) · fusion de deux univers rendue **détectable**, pas **implémentée** (hors scope assumé). **Restent MP-0c/d.**

**MP-0c (session 28, clos) — autorité du temps : le tick de simulation découplé de la frame de rendu** : troisième des 4 sous-jalons de MP-0. Spec `docs/plans/2026-09-05-mp0c-time-authority-design.md`, approuvée **4,4/5** après 2 tours (v1 à 3,7 NEEDS REVISION — le test d'équivalence échouait en `float` : `3f/60f ≠ 3f*(1f/60f)` d'1 ULP → 59 vs 60 ticks ; « les hashes de capture vont changer » était **faux** — aucun code prod ne lit `ctx.DeltaSeconds`, mode capture = 1 tick/frame). **`FixedTimestepAccumulator`** (`Agapanthe.Engine`, décision 7 : ni dans `FrameOrchestrator`, ni fusionné dans `SimulationHost`) accumule le dt wall-clock et pilote `SimulationHost.Tick` **N pas fixes** (`Advance` → nombre de ticks ; `AdvanceFrame` = `BeginFrame` + `Advance`, la séquence d'un hôte real-time). **Clamp d'entrée 250 ms** (anti-spiral-of-death) + garde de ratio ctor (`max/fixed > 1024` → throw : sinon `fixed≈1e-9` → boucle infinie, soustraction float = no-op). **Non-fini/négatif → `0f`** (perd la frame, échec inerte — jamais 15 ticks physique en silence) + `SanitisedInputCount` (signal Release, pas un `Debug.Assert` qui tuerait le test host). **`Sanitise` extrait** (patron R3, `float.IsFinite` **avant** `MathF.Min` qui propage NaN). **Déterminisme des captures** (décision 2, prédiction du brainstorm **corrigée** — pas une des 8 décisions) : `AGAPANTHE_MAX_FRAMES` (> 0) → dt synthétique constant = 1 tick/frame ; les hashes **doivent rester inchangés** — HDR `12638edd`, UI `03421357`, prouvé byte-identique sur 3 runs. **`FrameIndex` → `TickIndex`** partout (dette réservée par MP-0a) ; **off-by-one de `CurrentTick` corrigé** : `Math.Max(0L, TickIndex - 1)` = le **dernier tick réellement exécuté** (avant, un système render voyait `N+1`), épinglé par un test pour la première fois. **`PhysicsSystem.RatesMatch(float,float)`** extrait (R3) + `Debug.Assert` ; `PhysicsSettings.FixedDt` inchangé (décision 3). `SimulationHost.LastFrameTickCount` (latché en `EndFrame`, même cycle de vie que `LastFrameMs`) + loggé au banc Sandbox. **`AccumulatorEquivalenceTests`** : **20×`Advance(3·Fixed)` == 60×`Advance(1·Fixed)` == 60 ticks** (assertion **primaire = entiers** ; positions = garde de câblage car `PhysicsSystem` ignore `DeltaSeconds`) + catch-up `FrameOrchestrator`-shape via `AdvanceFrame`. **`AotComponentProbe`** exerce l'accumulateur (`AotAccumulatorSmoke: 1 + 3 ticks`). 558 tests, 0 warning, captures inchangées, `HeadlessSim` `7e8dc68f…` **JIT == AOT** inchangé, 0 leak / 0 validation. Double audit PASS-with-concerns ×2 (`engine-architect` 4,2/5), **aucun 🔴** — 8 findings 🟠/🟡 appliqués. **Dette laissée** (board §Deferred Work) : interpolation visuelle (l'accumulateur détient déjà le facteur de blend `_accumulated/FixedDeltaSeconds`) · `FixedTimestepAccumulator.Reset()` (Load in-process, pause) · **pas fixe = 4 littéraux `1f/60f` indépendants, prérequis netcode** de le rendre une donnée de config de la simulation · `bool HasTicked` (désambiguïse `TickIndex == 0`) · ancrage thread dans `Tick`. **Reste MP-0d.**

**Prochain (session 28+) — cap « vrai engine »** (backlog **§4quater**). La Vertical Slice a prouvé l'intégration : **VS-1 sérialisation** (S22), **VS-2 spawn runtime + gravité newtonienne** (S23), **VS-3 glu gameplay** (S24) — toutes CLOSES. **VS-4 (HUD) et VS-5 (audio) sont EN PAUSE** (décision humaine S25). Nouveaux ancrages : **l'artefact = le moteur** (pas un jeu) · généraliste **mais** spécialement sims spatiales grande échelle, un Stardew-like doit rester faisable · **multijoueur pensé dès maintenant, serveur autoritaire** (PAS lockstep : byte-identité intra-binaire seulement) · **massif/persistant visé, petite coop possible** → **la topologie est un choix de DÉPLOIEMENT, jamais d'architecture** (même code sim partout, seule l'autorité change ; ownership = concept dès le départ). MP-0 (fondations d'autorité) est **décomposé en 4 sous-jalons** : **MP-0a headless split** ✅ (S26) · **MP-0b identité** ✅ (S27, `GlobalId` 64 bits partitionné + `ContactPairKey`) · **MP-0c autorité du temps** ✅ (S28, fixed-timestep accumulator) · reste **🟠 MP-0d** (input → commandes horodatées). Puis `Agapanthe.App` → contenu (assets stables/cook/prefabs) → **2ᵉ slice dissemblable** → texte/UI, audio, queries physiques, job system → netcode. Détail + point de reprise dans `docs/AVANCEMENT.md` § Reprise.

**P3-M1 (session 14, clos)** : instancing via SSBO (draw calls **12 556 → 2** à 10k, batching par (matériau, mesh), `firstInstance`) + les **2 dettes de culling de M4 soldées** (`AggregateBounds` par frame, `ExtrudedShadowFrustum` ANDé au volume de lumière). Double audit PASS. 284 tests, 0 alloc/frame, AOT PASS.

**P3-M2 (session 15, clos)** : nouveau projet **`Agapanthe.Engine`** (marie World + Rendering, ne référence pas Platform, ne possède rien). L'**ordre de frame quitte le Sandbox** → `FrameOrchestrator` + `SystemScheduler` à étages (`Input→Simulation→PostSimulation→Render`), `Tick` hors `DrawFrame` (D1.a). **Lifecycle** : `Spawn`/`Despawn`/`SetParent`/`IsAlive` différés à une barrière, **`Despawn` cascade**, `EntityRef` = `GlobalId` (file de commandes propre au World, pas le `CommandBuffer` d'Arch). **Cull d'ombre deux passes** : wedge borné en amont (7ᵉ plan, `ShadowCasterDistance`), `UpstreamExtent` depuis les **casters** (plus les bounds globales). Double audit PASS, findings appliqués (garde F7 `_pass1ShadowList`). Banc `grid:100x100` Release+**AOT** : draws 2+2, 0 alloc/frame, 0 leak, **311 tests**, capture bit-identique `9790D95D`. Commit `90627a5`. Env var : `AGAPANTHE_CHURN=N` (stress lifecycle).

**P3-M3 → M8 (sessions 16-21, tous clos, double audit + verdict visuel PASS chacun)** : **M3** physique v1 (corps rigides linéaires déterministes, gravité, sphère↔sol/sphère, broadphase grille 0-alloc) · **M4** rendu GPU-driven (`scene_cull.comp` frustum-cull + compaction atomics, `vkCmdDrawIndexedIndirect`, offset de batch en push constant → MoltenVK-safe ; GPU visible == CPU) · **M5** CSM (4 cascades, atlas 2×2, `castsShadow` par entité) · **M6** slots persistants dirty-trackés (`PersistentInstanceBuffer`, gather+radix sort au rebuild structurel seulement, frame ordinaire O(dirty)) + cull d'ombre GPU (`shadow_cull.comp`) · **M7** buffers device-local (`CommandList.CopyBuffer` async, core `vkCmdCopyBuffer`) + raster d'ombre 4×→1× (7ᵉ plan near-cut, A+B ~15,3 → ~8,0 ms) · **M8** premier pas planétaire : **reversed-Z global** (`PerspectiveVulkanReversed`, comparateur depth **par pipeline** → passe d'ombre découplée) + `Primitives.UvSphere` + scène `AGAPANTHE_SCENE=planet` (planète + Soleil sphère emissive à **7,48e10 m**, échelle **1/2 uniforme**, **point light co-localisée avec la sphère-Soleil** = seule lumière, env noir). 333 tests, 0 alloc/frame, AOT PASS. Env vars : `AGAPANTHE_PHYSICS=1`, `AGAPANTHE_CULL_VERIFY=1`, `AGAPANTHE_SUN`, `AGAPANTHE_PLANET_{RADIUS,PHASE,ALT,FOV}`, `AGAPANTHE_SUN_{DIR,RADIUS,DISTANCE}`.

**VS-1 (session 22, clos) — sérialisation du World** : `GameWorld.Save/Load(Stream)`, snapshot **binaire blittable déterministe** (header `AGWD` + entités triées par GlobalId + `presenceMask` u32 sur l'ordre `ComponentRegistry.All` ; `InstanceSlot` exclu, `Parent`→GlobalId). GPU-free (aucun type Arch ne fuit), dispatch 3-switches concrets = AOT-safe, **pas de générateur** (blittable). Load sur World frais (compteur restauré sans bump, remap `Parent` par GlobalId, archétypes reconstruits fidèles). Robustesse : magic/version/count/tronqué/mask-hors-plage/GlobalId-dupliqué → `WorldSerializationException`. Seam GPU = **handles reproductibles** (Option 1, recharger les mêmes assets d'abord). Sandbox `AGAPANTHE_SAVE`/`AGAPANTHE_LOAD` (round-trip cross-process byte-identique JIT+AOT). Double audit PASS. **346 tests**, garde d'ordre `ComponentRegistry.All` figée + garde ≤32 composants.

**VS-2 (session 23, clos) — spawn runtime différé + gravité newtonienne** : `SpawnBodyDeferred` (jumeau runtime de `SpawnBody`, `CommandKind.SpawnBody` + 4 champs plats sur `StructuralCommand`, appliqué à la barrière structurelle, `MaterialiseBody` = point de matérialisation unique immédiat/différé) solde la dette P3-M3 (spawn de corps en cours de simulation sans muter les archétypes sous l'itération de `StepPhysics`). **Gravité newtonienne** minimale (décision humaine en interview) : attracteur unique **dans `PhysicsSettings`** (`WithAttractor(C, μ, R)`, pas un composant → ordre `ComponentRegistry.All` et masque VS-1 intouchés) ; `StepPhysics` branche sur `Mu>0` — gravité radiale inverse-carré `a=−μ·(p−C)/|p−C|³` (garde singularité `r2>1e-18`) + **sol radial** `|p−C|−r<R` (rest-clamp `2·(μ/R²)·dt`, PAS `gravity.Y`). **`μ=0` → chemin uniforme byte-identique** (scène `drop` protégée). Pas d'orbites (Euler+velocity `float`), pas de n-body. Sandbox `AGAPANTHE_SCENE=planet-drop` + `ProbeDropSystem` (Stage.Input) + keypress `B` ; profils Rider dans `Sandbox/Properties/launchSettings.json`. 355 tests, 0 alloc hot path, AOT PASS (`iterated 13`), capture headless déterministe. Double audit PASS-with-concerns (les 2 ont trouvé le **même 🟠** singularité `r2≈0`, corrigé) + verdict visuel PASS.

**VS-3 (session 24, clos) — glu gameplay : défi d'atterrissage planétaire** : câble **input → spawn (VS-2) → règle d'état → save/resume (VS-1)** sans nouveau chemin de rendu. Trois couches : le World gagne une **requête spatiale générique** `QuerySurfaceContacts(C,R,band,zoneC,zoneR) → LandingCounts(Total,Airborne,InZone)` (itère `BodyDesc`, 0-alloc prouvé par test, aucun type Arch ne fuit, « posé=SUR la surface » sans seuil de vitesse) ; la **règle pure latchée** `LandingChallengeRule.Evaluate(counts, shotsIssued, prev)` dans `Agapanthe.Engine` (Won si `InZone≥N`, Lost si budget épuisé **et** `Airborne==0`, terminal monotone intra-session) ; le `LandingChallengeSystem` (PostSim, Camera + `window.Title`) dans le Sandbox. Boucle `AGAPANTHE_SCENE=planet-challenge` : `B` largue une probe **radiale sous la caméra** (chute newtonienne VS-2), poser **N=3** en **≤ M=6** tirs ; `TryShoot` gardé par `_shotsIssued<M` (pas la query) ; titre reconstruit seulement sur changement `(InZone,shots,status)` → 0-alloc régime stable. Beacon émissif = drawable (pas un body). **Save/resume relaunch-only** (`F5`→Save flush intégré ; `AGAPANTHE_LOAD`) : **zéro octet ajouté au snapshot VS-1** — `_shotsIssued` re-semé du body count post-Load, statut re-évalué ; constantes de scène épinglées dans les profils Rider `planet-challenge`. 372 tests (17 GPU-free), 0 warning, headless 0 validation/0 leak (226 resources), AOT PASS. Double audit PASS / PASS-with-concerns 4/5 + verdict humain PASS.

**Dette persistante** : ✅ ~~`CurrentTick.FrameIndex` post-incrément~~ **résolu MP-0c** (`FrameIndex`→`TickIndex`, `CurrentTick` = `Math.Max(0L, TickIndex-1)` = dernier tick exécuté, épinglé) · 🟠 **seam `FrameProfiler` reporté à UI-3** (UI-2) — les timestamps GPU arrivent à N+2 et casseront `FrameStats.Record(float, long)` en désalignant les séries ; le refactor appartient au jalon qui en connaîtra la forme, et `DebugOverlaySystem` reste sans tests pour la même raison (dépendance à l'orchestrator concret) · 🔴 **Linux jamais validé** (premier item P3, non bloquant pour la Vertical Slice) · 🟠 **latch Won/Lost VS-3 non-monotone à travers un reload** (`_status` re-dérivé du monde, zéro octet au snapshot ; un probe glissant hors/dans la zone entre save et reload peut un-win/un-lose — faible proba ; alternative `.state` 1 octet déférée) · 🟠 **CSM `_casterSpheres` dans le World** (F7, une `RenderView`/frame — gardée) · 🟠 **`UpstreamExtent` par cascade** déféré (near-cut calé sur épaisseur de tranche, pas longueur d'ombre — soleil très rasant = cas limite ; backlog §2.0bis) · 🟠 **pas de lifetime/rest-cull des corps runtime** (VS-2 m2 : le spawner de la démo fait croître les entités sans borne → politique de durée de vie nécessaire pour l'ancre persistante) · 🟠 cascade despawn O(profondeur×N) quand un despawn est en attente · 🟠 `SortKey` sans profondeur (+ compaction atomique scramble l'ordre → 2ᵉ verrou transparence) · 🟡 garde normalisation `Frustum` `1e-8` (near sub-mètre) · plafonds **16 bits** mesh/matériau et **`GlobalId < 2³²`** (clé de contact physique) à faire échouer bruyamment · 🟡 **sérialisation (VS-1) : `Generation` des handles non validée au load** → un ordre de chargement d'assets différent casse en silence (futur *fingerprint d'assets* dans le header) · device-local/transfer **non exécuté sur MoltenVK** (P3-M0) · crash shutdown GLFW/Silk.NET via `AGAPANTHE_UNLOAD_TEST=20` (~2 runs/10, après le rapport propre).
