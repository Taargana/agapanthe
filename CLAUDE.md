# Agapanthe — contexte projet (à lire en premier)

Moteur de jeu **Vulkan en C# from scratch** (.NET 10), cross-platform (Windows / Linux / macOS).
**Phase 1 (TERMINÉE, 8/8)** : chaîne graphique 3D. **Phase 2 (TERMINÉE, 5/5)** : fondations scalables — ECS (Arch), `double` + camera-relative à origine quantifiée, culling, montée en charge (10k entités à 10 000 km, NativeAOT). **Phase 3 (en cours)** : gameplay — **P3-M1 instancing** ✅, **P3-M2 scheduler + lifecycle + `Agapanthe.Engine`** ✅, **P3-M3 physique v1** ✅, **P3-M4 rendu GPU-driven** ✅, **P3-M5 CSM** ✅, **P3-M6 slots persistants + cull d'ombre GPU** ✅, **P3-M7 device-local + raster ombre 4×** ✅, **P3-M8 premier pas planétaire (reversed-Z + scène planète/Soleil 1/2)** ✅, **VS-1 sérialisation du World (save/load snapshot)** ✅, **VS-2 spawn runtime différé + gravité newtonienne** ✅, **VS-3 glu gameplay (défi d'atterrissage planétaire)** ✅, **UI-1 texte à l'écran** ✅, **UI-2 overlay debug + profiler CPU** ✅, **MP-0a headless split** ✅, **MP-0b identité d'entité** ✅, **MP-0c autorité du temps (fixed-timestep accumulator)** ✅, **MP-0d input → commandes horodatées** ✅ ; **MP-0 CLOS (4/4)** ; **`Agapanthe.App`** ✅ (S30, premier hôte de prod) ; **Contenu-1** identité d'assets stable ✅ (S31), **Contenu-2** cook offline + manifest ✅ (S32), **Contenu-3a** `AssetRef` sim-side + snapshot v4 + scission `SceneContext` ✅ (S33), **Contenu-3b** `.agscene`/`.agprefab` TOML→blob cuit + `SceneLoader` + `Agapanthe.Scene` ✅ (S34) — **Contenu CLOS (3/3)**, **Contenu-3c-1** registres de fabriques + migration `planet`/`planet-drop` ✅ (S35), **Contenu-3c-2** `LandingChallenge` + migration `planet-challenge` + correctif générique de reprise ✅ (S36), **Contenu-3c-3** `ground_quad` + migration `drive` + nettoyage final ✅ (S37) — **Contenu-3c CLOS (3/3), domaine Contenu entièrement clos** ; **Slice-2** 2ᵉ slice dissemblable — `samples/TopDown`, caméra orthographique réelle, `Agapanthe.Platform.App` partagé ✅ (S38) ; **UI-3** timestamps GPU + seam `FrameProfiler` ✅ (S39) — **Texte & UI ENTIÈREMENT CLOS (3/3)** ; **queries physiques — raycast + layer mask** ✅ (S40, `GameWorld.TryRaycast`/`RaycastAll` + `QueryLayer` + `Camera.ScreenPointToRay`) ; **queries de formes — sphere overlap** ✅ (S41, `GameWorld.OverlapSphere`) ; **queries de formes — box overlap (AABB)** ✅ (S42, `GameWorld.OverlapBox`) ; **job system, fondations (Job-1)** ✅ (S43, parallélisme inter-systèmes sur `Stage.Simulation`) ; cap : **« vrai engine »** (backlog §4quater — VS-4/VS-5 en pause) → prochain item §4quater (audio, netcode, Job-2/Job-3) ; validation Linux/macOS (P3-M0) toujours dûe.

## Où est la vérité (lis ces fichiers avant d'agir)

- **`docs/AVANCEMENT.md`** — état d'avancement, jalons, métriques, plan du prochain jalon, **section « Reprise — où repartir »**. C'est le point d'entrée pour reprendre le travail.
- **`docs/BACKLOG.md`** — ce qu'on sait devoir faire *plus tard*, avec le pourquoi et **quand ça mord** (CSM, rendu GPU-driven, nuages volumétriques, atmosphère, ombres analytiques planétaires, physique…). À lire avant de choisir un jalon ; à mettre à jour quand une décision technique est instruite.
- **`docs/plans/2026-07-02-graphics-engine-design.md`** — spec de référence de la phase graphique (§ numérotés, cités partout).
- **`.absolute-human/board.md`** — board de la session courante ; sessions passées dans `.absolute-human/archive/board-sessionN-*.md`.
- **`docs/visual-checks/`** — protocoles de validation visuelle par jalon (verdicts humains).

> La mémoire persistante de Claude (`~/.claude/…/memory/`) **ne voyage pas avec le dépôt**. Sur un nouvel appareil, tout le contexte nécessaire est dans les docs ci-dessus — commence par `docs/AVANCEMENT.md`.

## Décisions structurantes (verrouillées — ne pas re-litiger)

- Bindings **Silk.NET** (Vulkan + GLFW + input) ; le reste from scratch.
- Baseline **Vulkan 1.2 + dynamic_rendering + synchronization2** (chemin 1.3 core sur MoltenVK).
- Maths **System.Numerics** (convention row-vector) + helpers clip-space Vulkan (Y-flip, Z [0,1]).
- Shaders **GLSL → SPIR-V à l'exécution (shaderc)**, cache disque.
- Couche GPU **mince mono-backend** : **aucun type `Vk*` ne sort de `Agapanthe.Graphics`**.
- Allocateur GPU from scratch ; **IDisposable partout**, destruction différée N+2 frames (DeletionQueue non-capturante), **zéro alloc managée par frame** sur le hot path, **ResourceTracker (tout leak = échec du run)**.
- **Tout message de validation layer = bug.** `.NET 10`, `TreatWarningsAsErrors`.
- MoltenVK portability : pas de comparateur mutable, pas d'imageCubeArray (vérifier chaque feature au premier VUID).

## Modules

```
Core ◄── World ◄── Engine                       LA SIMULATION — headless, closure = {Core, World}
                     ▲                          (aucun Rendering / Graphics / Platform / Silk.NET :
                     │                           EngineIsHeadlessTests l'impose, 6 cas)
Core ◄── Graphics ◄── Rendering ◄── Engine.Render   la moitié rendu (compose un SimulationHost)
                                        ▲
                    Engine.Render + Platform ◄── App   composition-root : AppHost (boucle client,
                                        ▲                bootstrap GPU, teardown 0-leak) + contrat IGame,
                          Sandbox (fenêtré)             ISceneRecipe.Build(SimSceneContext, PresentationSceneContext?)
                                  ·                     — SimSceneContext headless-safe (Contenu-3a, gate réflexif).
                          HeadlessSim (serveur, NativeAOT, sans GPU)   App NE référence PAS Platform ; EngineIsHeadlessTests pin App.csproj.
Rendering ──► Ui ──► Assets ──► Core            (Ui et Assets GPU-free ; le runtime ne parse plus glTF)
                       ▲
              Assets.Pipeline (cook-side, PAS AOT — glTF + StbImageSharp + writers .agmodel/manifest)
                       ▲
              tools/AssetCooker (Exe) ·  tests    (jamais référencé par un projet shippé)
Platform ──► Core                               (fenêtre GLFW, input — jamais référencé par Engine ni par App)
```
`Agapanthe.Graphics` reste le **seul** projet référençant Silk.NET.Vulkan ; aucun type `Vk*` n'en sort.
`Agapanthe.App` peut référencer tout le moteur **sauf `Agapanthe.Platform`** (sinon Vulkan entrerait
transitivement dans Platform, un leaf) — `EngineIsHeadlessTests` l'impose par allowlist statique.

## Commandes

```sh
# prérequis macOS : brew install molten-vk vulkan-loader vulkan-validationlayers vulkan-tools
dotnet build
dotnet test
# Sandbox (le loader Vulkan Homebrew est hors des chemins GLFW par défaut → DYLD_LIBRARY_PATH)
DYLD_LIBRARY_PATH=/opt/homebrew/lib dotnet run --project samples/Sandbox
DYLD_LIBRARY_PATH=/opt/homebrew/lib dotnet run --project samples/Sandbox -- MetalRoughSpheres.glb
```

Env vars debug : `AGAPANTHE_MAX_FRAMES=N` (auto-close, sortie 0 si 0 leak) · `AGAPANTHE_CAPTURE=out.ppm` (dump HDR tonemappé) · `AGAPANTHE_VIEW="x,y,z"` (angle caméra) · `AGAPANTHE_HDRI=<path.hdr>` (environnement IBL) · `AGAPANTHE_IBL_TEST=<préfixe>` (génère l'IBL headless + dump faces/maps) · `AGAPANTHE_SHADER_RELOAD_TEST=1` (force un reload des 4 passes + logge le wall-time).

> **MP-0c — déterminisme des captures** : depuis le fixed-timestep accumulator, un run déterministe (capture, banc)
> **DOIT** poser `AGAPANTHE_MAX_FRAMES` (> 0). Ce flag seul bascule le Sandbox sur un dt synthétique constant (= le
> pas fixe, 1 tick/frame) ; sans lui l'accumulateur consomme le vrai dt wall-clock GLFW et le run n'est plus
> reproductible run-à-run. `AGAPANTHE_CAPTURE`/`AGAPANTHE_CULL_STATS` seuls ne suffisent pas.

**Hot reload shaders** (M8) : éditer un fichier de `shaders/` pendant que le Sandbox tourne → recompile + recréation du pipeline en < 1 s. Échec de compile = log + ancien pipeline conservé. Limites : éditions **interface-compatibles** uniquement (changer un binding → restart) ; les 4 shaders compute IBL ne sont pas surveillés.

## Méthode de travail

Sessions pilotées via **absolute-human** (décomposition en tâches, vagues parallèles, TDD). Chaque jalon clôt sur : Sandbox propre (0 message validation, 0 leak), tests verts, **double audit agent** (`csharp-lowlevel` + `engine-architect`) PASS, protocole visuel humain quand pertinent, board archivé.

## Préférences de travail (l'humain)

- **Commits sur demande uniquement** : ne jamais `commit`/`push` sans demande explicite. L'humain pilote le rythme et valide chaque étape.
- **Travail en vagues + feu vert** : décomposer en vagues, attendre l'accord de l'humain entre les étapes, lancer les audits subagents (`csharp-lowlevel` + `engine-architect`) avant de clore un jalon.
- **Vérification visuelle headless** : captures headless (`AGAPANTHE_CAPTURE`) à chaque vague pour prouver le rendu ; **verdict visuel humain** requis avant de marquer un jalon `done`.
- **Gate 0 leak / 0 validation** : tout message de validation layer ou tout leak ResourceTracker est **bloquant**, jamais ignoré ni contourné.
- **Langue** : conversation en **français** (phrases complètes) ; **code, commits, PRs et docs techniques en anglais**. Dans la prose française, les **termes techniques du domaine restent en anglais** (shader baking, culling, frustum, shadow mapping, tie-break, radix sort, lockstep, instance buffer, server meshing…) — ne pas les traduire (« précuit » pour *baking* est à proscrire).

## État courant

**PHASE 1 CLOSE (8/8). PHASE 2 CLOSE (5/5) — P2-M0 → P2-M4 clos** (gate AOT + Arch · SPIR-V hors-ligne · couture ECS · camera-relative · **frustum culling + montée en charge**). Métriques : 275 tests, 0 warning, 0 message de validation, 0 leak, probe NativeAOT PASS. Double audit signe la clôture de la phase.

**P2-M4 (session 13)** : le monde stocke les positions en `double` à **origine quantifiée** (snap 1024 m), ne dessine que ce que voit la caméra (frustum culling), et le prouve à l'échelle — **10 000 entités cullées à 10 000 km, 0 alloc/frame, en NativeAOT**, image bit-identique à l'origine (maille alignée). Env vars : `AGAPANTHE_WORLD_ORIGIN="x,y,z"` · `AGAPANTHE_SCENE=grid:NxN` (banc) · `AGAPANTHE_CULL_STATS=1` (stats) · `AGAPANTHE_UNLOAD_TEST=N`.

**UI-1 (session 25, clos) — texte à l'écran** : premier des 3 jalons Texte & UI (spec `docs/plans/2026-08-03-text-ui-design.md`, approuvée 4,4/5 après 2 tours de revue scorée). **Rasterisation SDF HORS-LIGNE** : `tools/FontCooker` (console non-AOT, jamais référencée par un projet shippé) utilise **`StbTrueTypeSharp`** (pur managé, sœur de `StbImageSharp`) → **zéro dépendance native, y compris dans l'outil** ; FreeType écarté (n'apporte que du hinting, inutile en SDF). Format **`.agfont`** binaire déterministe (patron VS-1) lu par `Agapanthe.Assets`. Nouveau projet **`Agapanthe.Ui` GPU-free** (`UiDrawList`, shaping avec seam `Rune` pour HarfBuzz plus tard, layout, `Measure`) ; **`Rendering` référence `Ui`** (forcé : `StorageBufferRing<T>` est `internal` à Rendering). Rendu : **1 pipeline / 1 atlas / 1 draw call**, SSBO + `gl_VertexIndex` sans vertex buffer, un **set per-frame**, après le tonemap en `LoadOp=Load`, blend **premultiplied** (nouveau `BlendMode` — le blending était câblé en dur à `false`). Ordre shader obligatoire : sRGB→linéaire **puis** ×alpha (l'inverse = halo). Atlas `R8Unorm`, **`MipLevels=1`** (les mips lisseraient le champ de distance). **Capture swapchain** ajoutée (`AGAPANTHE_CAPTURE_UI`) : `AGAPANTHE_CAPTURE` lit la cible HDR et ne peut donc **pas** voir un overlay dessiné après le tonemap. 435 tests, 0 warning, 0 validation, 0 leak, AOT PASS (`StbTrueTypeSharp` absent du publish), hashes `12638edd` (non-régression blending, inchangé) / `6e14b23e` (capture UI). Double audit PASS-with-concerns ×2 + verdict humain PASS. Env vars : `AGAPANTHE_CAPTURE_UI=<path.ppm>`.

**UI-2 (session 25, clos) — overlay debug in-view + profiler CPU** : deuxième des 3 jalons Texte & UI. `FrameStats`/`FrameSeries` (rings circulaires frame time + octets alloués, agrégats sans alloc) et `DebugOverlaySystem` (`ISystem` en `PostSimulation`, bascule **`F3`**, `AGAPANTHE_OVERLAY=0` pour démarrer masqué) dans `Engine` ; `Sparkline` (primitif de graphe GPU-free) et `TextBuilder` (formatage 0-alloc `TryFormat`+`stackalloc`, **public**) dans `Ui`. L'overlay **remplace le HUD `window.Title`** et son hack de cession VS-3 (`&& landingChallenge is null`) — `Program.cs` perd 79 lignes. **Le gate 0-alloc devient visible en continu à l'écran** (`alloc 0 B/frame` en vert, rouge dès qu'une frame alloue). **La leçon du jalon est la fenêtre de mesure, pas le compteur** : l'overlay a d'abord rapporté 272 B/frame là où le banc disait 0 (sa fenêtre englobait le pump d'événements GLFW, hors contrôle du moteur), puis la correction fermait le bracket **dans** `RecordCommandBuffer` — avant submit/present — et **jamais** sur les frames à sortie précoce (resize), affichant « 0 B » en vert pendant qu'une swapchain était recréée par frame. Le bracket vit désormais dans `FrameOrchestrator` (`Tick` ouvre, **`EndFrame()` après `DrawFrame`** ferme), exactement celui du banc ; `FrameStats.Record(float, long)` **reçoit** les mesures au lieu de les échantillonner. `GC.GetAllocatedBytesForCurrentThread()` est **par thread** → `Debug.Assert` d'ancrage mono-thread (MP-0 est le jalon qui casserait ça). Échelles de graphe calées sur la **frame courante**, pas sur le `Max` de la fenêtre (un pic de warm-up à 400 Kio rendait une régression de 512 B/frame invisible 4 s). Une **ligne = un `DrawText`** (plafond 256 glyphes). Un profiler affichant des timings réels ne peut pas être byte-identique → le gate déterministe porte sur l'**overlay masqué** (`03421357`), l'overlay lui-même sur verdict humain (diff inter-runs 432 px / 921 600, confiné au panneau). 468 tests, 0 warning, 0 validation, **0 hazard**, 0 leak, AOT PASS. Double audit PASS-with-concerns ×2 (aucun 🔴). ~~**UI-3 (timestamps GPU) reste à faire.**~~ ✅ **livré S39**, voir plus bas. Env var : `AGAPANTHE_OVERLAY=0`.

**UI-3 (session 39, clos) — timestamps GPU + seam `FrameProfiler` — Texte & UI ENTIÈREMENT CLOS (3/3)** : dernier des 3 jalons Texte & UI. Spec `docs/plans/2026-09-13-ui3-gpu-timestamps-design.md`, approuvée **4,15/5** après 2 tours (round 1 : 1 écart factuel + 1 vrai trou de conception, tous deux corrigés). Ferme la dette explicite d'UI-2 : les timestamps GPU arrivent ~2 frames en retard (`FramesInFlight=2`) et auraient désynchronisé `FrameStats`/`FrameSeries` (append-only, 0-alloc, testées) si retrofit naïvement — **résolu par découplage total**, pas par retrofit : `FrameStats`/`Engine` restent intouchés (`git diff` vide), la série GPU vit entièrement côté `Agapanthe.Rendering`/`Agapanthe.Engine.Render`. Nouveau **`QueryPool`** (`Agapanthe.Graphics`, `VkQueryPool` timestamp, disposal différé via `DeletionQueue` — patron `Sampler`) instrumente les **4 régions debug-label existantes** (`Shadow`/`Scene`/`Tonemap`/`UI`, déjà délimitées par des `PushDebugLabel`, aucune nouvelle région introduite) via `CommandList.WriteTimestampBegin`/`End` (`vkCmdWriteTimestamp2`, dispatch sync2 miroir de `CmdPipelineBarrier2`) + `ResetQueryPool`. **Détection de capacité par la queue graphique réellement utilisée** (`TimestampValidBits`, pas le feature bit global `timestampComputeAndGraphics` — précaution MoltenVK) : `GraphicsDevice.SupportsGpuTimestamps` ANDé avec `HostOptions.GpuTimestampsEnabled` (`AGAPANTHE_GPU_TIMESTAMPS=0`, patron `OverlayVisible`) → `Renderer.SupportsGpuTimestamps`. `Renderer` possède le pool et lit en retour **une fois par frame, non-bloquant, par région indépendamment** (`WITH_AVAILABILITY_BIT`, jamais `WAIT_BIT`) dans `GpuPassTimingsMs` (4 champs `float?` — un champ `null` = région non exécutée ce cycle, ex. `UI` sans rien à dessiner, ou en principe pas encore prête ; jamais un stall, jamais une valeur fabriquée). `DebugOverlaySystem` affiche une ligne par-passe (`gpu shadow X.XX scene X.XX tonemap X.XX ui X.XX ms`, placeholder `--` pour un champ absent — jamais confondu avec un vrai `0.00`) + un graphe pour le total ; absence propre quand non supporté (panneau plus court). **`GpuTimestampMath.TryToMilliseconds`** (pure, GPU-free, testée — patron `MathHelpers`/`ShadowFit`) convertit les ticks bruts en ms, masque aux `timestampValidBits` valides et rejette le wraparound/non-fini plutôt que de fabriquer une valeur. **861 tests**, 0 warning, capture masquée (overlay invisible) **byte-identique avant/après** (`git stash` A/B, `b5382ac6…`) — preuve que l'instrumentation ne touche aucun pixel du rendu — les 9 captures HDR (8 scènes Sandbox + `topdown`) inchangées, verdict visuel humain PASS sur l'overlay visible (nombres par-passe plausibles) et sur le chemin dégradé (`AGAPANTHE_GPU_TIMESTAMPS=0`, panneau visiblement plus court), **JIT == NativeAOT** sur les 3 binaires, 0 leak / 0 validation partout.

Double audit `csharp-lowlevel` (3,4/5) + `graphics-3d` (3,4/5) — déviation assumée du duo standard, décidée dès le pré-spec S25 (travail neuf de query pool Vulkan). **3 🔴 trouvés** (2 indépendamment par les deux, 1 par `graphics-3d` seul — plus sévère qu'un 🟠 initial de `csharp-lowlevel` sur le même sujet) — **tous corrigés** : (1) **inversion de slot** — la 1ʳᵉ version lisait le slot **en vol** (`frame.Slot - 1`) au lieu du slot que `FrameRenderer` vient de garantir terminé via `WaitForFences(_inFlightFences[_frameSlot])` — race hôte/GPU possible, appariement croisé begin/end de deux frames différentes produisant un delta positif plausible mais faux, non attrapable par le garde de wraparound ; corrigé en lisant `frame.Slot` avant de le reset. (2) **`TOP_OF_PIPE` en begin** — équivaut à `NONE` dans un premier scope synchronization2 (n'attend rien) : les 4 régions latchaient quasi simultanément, produisant des cumuls (`Total` jusqu'à ~4× le vrai coût GPU) au lieu de durées indépendantes ; corrigé en `ALL_COMMANDS` aux deux bornes. (3) **lecture de queries jamais reset** sur les 2 premières frames (`VUID-vkGetQueryPoolResults-None-09401` — violation systématique invisible seulement faute de SDK de validation à jour) ; corrigé par un compteur `_gpuTimestampFramesSeen` qui ne fait que reset les 2 premiers appels. 5 findings 🟠 supplémentaires appliqués : `TimestampValidBits` testé puis jeté (Intel/AMD/MoltenVK rapportent couramment 36-40 bits contre 64 pour NVIDIA — masquage explicite + garde de finitude ajoutés à `GpuTimestampMath`) · `QueryPool.ReadResultsNonBlocking` sans bornes/garde disposed/arithmétique overflow-safe (4 gardes ajoutées) · 4 `FrameSeries` par-région écrites chaque frame et lues par personne (patron `AggregateBoundsSystem`, déjà fermé une fois par MP-0a — supprimées) · total enregistré à `0` franc quand les 4 champs sont `null` (indiscernable d'une frame réellement gratuite — ne s'enregistre plus que si au moins un champ est disponible) · suffixe `ms` manquant sur la ligne de texte. `graphics-3d` a aussi documenté **4 risques MoltenVK réels pour P3-M0** (versés au backlog, inactionnables sans matériel Apple) : `stage` entièrement ignoré par MoltenVK · granularité per-encodeur-Metal sur Apple Silicon · fallback CPU à zéros francs · 2 bugs MoltenVK ouverts sur exactement ce patron double-buffered. **Dette laissée** (versée au backlog §4quater) : 4 risques MoltenVK ci-dessus · pas de couverture de test du chemin matériel-réellement-non-supporté (seul le chemin explicitement-désactivé est testable sur cette machine) · le `Total` du graphe ne couvre pas la synchronisation de la copie candidats/les transitions swapchain (hors des 4 régions instrumentées, documenté). **Verdict visuel humain : PASS** (2026-09-13). Env var : `AGAPANTHE_GPU_TIMESTAMPS=0`.

**Queries physiques — raycast + layer mask (session 40, clos)** : premier item §4quater après la clôture entière du domaine Texte & UI. Spec `docs/plans/2026-09-14-physics-queries-raycast-design.md`, approuvée **4,2/5** après 3 tours (round 1 : précédent fabriqué pour `QueryLayer` — un split `WithNone<T>()` inexistant dans ce codebase, corrigé pour citer le vrai précédent `NoShadowCast` ; round 2 : la correction avait elle-même fabriqué un `ImportedEntitySpec.CastsShadow` inexistant, corrigé). **`GameWorld.TryRaycast`/`RaycastAll`** (nouveau `GameWorld.Queries.cs`, sibling de `.Physics.cs`) — contre **tout drawable** via le composant `Bounds` déjà existant (pas seulement les `RigidBody`, D2), masque de layer optionnel via **`QueryLayer { uint Mask }`** (composant #14, absent ⇒ `AllLayers`, résolution single-query + `Has<QueryLayer>()` inline — mirrors `NoShadowCast`, PAS un split deux-queries). Nouvelle grille de broadphase (`Agapanthe.World`, séparée de la grille physique `_cellHead`/`_cellNext` de `StepPhysics`), reconstruite à chaque appel, DDA Amanatides-Woo sur un voisinage 3×3×3. `Ray`/`RaySphereIntersect` (`Agapanthe.Core`, nouveaux, résolus en `double`). `ImportedEntitySpec.Layer` (nouveau champ nullable, atteint `SpawnImported` **et** `SpawnDeferred`/`SpawnBodyDeferred` — contrairement à l'ancien `castsShadow` qui n'est qu'un paramètre de `SpawnImported`). Authoring TOML `layer` sur `[[entity]]` (`.agscene` v5→v6, rejeté explicitement sur `[[grid]]`/`[[cluster]]`, mirroring le précédent `body`/`velocity` de Contenu-3c-3). **`Camera.ScreenPointToRay`** (`Agapanthe.Rendering`) — perspective **et** orthographique (un premier jet ne gérait que la perspective, trouvé et corrigé par le double audit, cf. ci-dessous). Démo Sandbox+TopDown : `Key.F` = crosshair écran-centre (D6 réinterprété — `IWindow` n'expose aucun événement de clic ni position de curseur brute, seulement capture-au-clic + delta pendant la capture ; câblé une fois au niveau `AppHost`, s'applique aux deux apps gratuitement).

**Découverte imprévue en cours de Wave 1** : ajouter `QueryLayer` (composant #14) a fait échouer 43 tests en apparence sans rapport — un `Debug.Assert` inconditionnel dans `WorldSerialization.cs` comparait le compte de composants live contre une constante figée à 13 (v4). Corrigé par un bump de format **v4→v5**, purement additif (contrairement au vrai remaniement de données v3→v4), miroir du précédent établi ; les 3 hashes `HeadlessSimSnapshotFormatTests` re-épinglés (longueurs inchangées). Second imprévu, trouvé pendant PQ-012 : `CookRunner.CookerVersion` pas bumpé pour le saut `.agscene` v5→v6 — même classe de bug qu'un finding déjà audité en Contenu-3c-2, corrigé.

**904 tests** (+45 depuis UI-3, dont 14 ajoutés par le fix d'audit du DDA/quadratique), 0 warning, 0 régression. Les 9 captures pinnées (8 scènes Sandbox + `topdown`) re-vérifiées **byte-identiques** par preuve A/B `git stash` contre le point de rollback `974384a` (5 d'entre elles tombent exactement sur les hashes déjà épinglés dans ce document, confirmant que les paramètres de repro étaient corrects). `HeadlessSim`/Sandbox **JIT == NativeAOT** confirmés avant **et** après la passe de correctifs d'audit (`dbe9ed9103f10f4a34708e174753721b`, inchangé). **Verdict visuel humain PASS** (scène `grid`) : `F` sur une entité visible → `[raycast] hit entity EntityRef(94) at distance 9.19/9.21/9.58 m` (stable), `F` vers le vide → `[raycast] no hit`.

Double audit `csharp-lowlevel` (**3,6/5**, PASS-with-concerns) + `engine-architect` (**4,1/5**, PASS-with-concerns) — paire standard du projet (aucune nouvelle surface Vulkan, pas de déviation `graphics-3d`). **Forte convergence** : 3 findings trouvés indépendamment par les deux (bug orthographique de `ScreenPointToRay`, coût de marche DDA non borné, sémantique de troncature de `RaycastAll` mal documentée). **1 🔴 trouvé-et-corrigé** (`csharp-lowlevel` seul) : `maxDistance = double.PositiveInfinity` passait le garde NaN/`≤0` existant et faisait boucler la marche DDA indéfiniment (`while (t <= maxDistance)` ne termine jamais contre `+Infinity`) — un appel parfaitement naturel (« cast aussi loin que possible ») rendait le process infrangible ; corrigé en rejetant `IsInfinity` au même point de garde. Findings 🟠 corrigés : coût de marche non borné → désormais bornée à l'AABB des cellules occupées calculée pendant la construction de la grille, `TryRaycast` réduit son rayon de recherche à `bestDistance` dès qu'un hit est trouvé, `RaycastAll` sort dès que son buffer est plein · `RaycastHit.Point` narrowait la distance `double` exacte en `float` avant de multiplier, contredisant la prémisse « résolu en double » — recalculé en arithmétique `Double3` exacte · une direction dégénérée (NaN/nulle) pouvait empoisonner `bestDistance` avec `NaN` ou planter via une `ArithmeticException` non documentée — rejetée par `ArgumentException` aux deux points d'entrée (plus robuste que l'assert Debug-only initialement prévu par le spec) · le tie-break à distance égale dépendait de l'ordre d'archétype Arch (non déterministe) — passé à un `<` strict (premier rencontré gagne) · `RaySphereIntersect` annulait catastrophiquement au-delà de ~1e8 m — réécrit avec la formule numériquement stable du point le plus proche, vérifié à 7,48e10 m (l'échelle `AGAPANTHE_SUN` du moteur lui-même) · le bug orthographique de `ScreenPointToRay` ci-dessus, désormais implémenté correctement (rayons parallèles, origine décalée par pixel) au lieu de produire silencieusement un cône perspective faux — la fausse affirmation « projection-agnostic » du spec §7 corrigée · `EnsureStampCapacity` réallouait à chaque appel dès qu'un monde dépassait sa taille initiale (exactement la forme `planet-drop`/`planet-challenge`) — croissance par doublement désormais, comme sa jumelle · les 2 sites de copie champ-à-champ de `ImportedEntitySpec` restants (`ProbeDropSystem`/`LandingChallengeSystem`) perdaient silencieusement `Layer` — corrigé (no-op aujourd'hui, aucun gabarit ne peuple `Layer`, mais ferme la couture que D3 visait précisément) · `layer = 0` authoré était silencieusement inatteignable par toute query y compris `AllLayers` — rejeté au cook, conforme à la convention du projet sur les valeurs authorées dégénérées. **Dette laissée** (🟡 acceptée) : aucune fixture de test dédiée au format snapshot v4 · l'assert de tripwire de compte de composants vit dans `Load` et pas seulement `Save` · dégénérescence pré-existante de `Camera.Right` à pitch ±90° (hors scope) · tolérance d'angle un peu large sur un test (la convention sous-jacente est correcte et épinglée ailleurs) · micro-optimisation possible (`Has<QueryLayer>()` par-chunk plutôt que par-entité).

**Queries de formes — sphere overlap (session 41, clos)** : deuxième item §4quater après les queries physiques (raycast + layer mask, S40). Spec `docs/plans/2026-09-14-shape-queries-overlap-design.md`, approuvée **4,4/5** après 2 tours (round 1 : 3,4/5 NEEDS WORK — un vrai défaut de cohérence interne : le raisonnement justifiant la réutilisation du mécanisme de dédup (`_qVisitedStamp`/`_qStamp`) était **faux** — il prétendait qu'un candidat pouvait être bucketé dans plusieurs cellules par `BuildGrid`, ce qui n'arrive jamais ; le vrai correctif — une expansion de la plage de cellules d'une marge d'une cellule, sans dédup nécessaire pour ce balayage non-marchant — a été identifié et vérifié indépendamment par le round 2, qui a re-dérivé le calcul de marge et l'a confirmé exact). **`GameWorld.OverlapSphere`** (nouveau, dans `GameWorld.Queries.cs`, sibling de `TryRaycast`/`RaycastAll`) — « qu'y a-t-il dans cette sphère ? », réutilise **intégralement, sans modification**, `GatherCandidates`/`BuildGrid` de S40 (la décision D4 du raycast avait explicitement anticipé cette réutilisation — confirmé par `git diff` : 0 ligne touchée dans ces deux méthodes). Nouveau **`OverlapHit(EntityRef, double Distance)`** (pas de `Point`, contrairement à `RaycastHit` — une query de région n'a pas de point d'intersection unique, D2). Démo `Key.G` (Sandbox+TopDown, même point de câblage que `Key.F` de S40) : `world.OverlapSphere(camera.Position, 10f, ...)`, log le compte + chaque entité/distance. **922 tests**, 0 warning, 0 régression, les 9 captures pinnées re-vérifiées byte-identiques par preuve A/B `git stash` (toutes tombent exactement sur les hashes déjà connus de S40), Sandbox JIT == NativeAOT confirmé avant et après la passe de correctifs d'audit, verdict visuel humain PASS.

Double audit `csharp-lowlevel` (**3,8/5**) + `engine-architect` (**4,0/5**), tous deux PASS-with-concerns — convergence très forte : les deux ont trouvé indépendamment le même risque de classe 🔴 et le même bug pré-existant dans du code déjà livré. **1 🔴 trouvé-et-corrigé** : le balayage de cellules d'`OverlapSphere` n'était borné par rien lié à la query elle-même — `cellSize = 2 × rayon-max-candidat` est dicté par le plus gros objet du monde, pas par le rayon de la query, donc un appel parfaitement naturel « qu'y a-t-il dans 1 km ? » sur un monde dense en petits objets pouvait balayer des milliards de cellules vides. Corrigé par une estimation du volume balayé en `double` **avant** tout calcul `long`, avec repli sur un scan linéaire direct du tableau de candidats déjà rassemblé dès que la grille balaierait au moins autant de cellules qu'il y a de candidats — ferme aussi, comme effet de bord, un débordement `long` silencieux. **Un vrai bug pré-existant trouvé dans `RaycastAll`** (livré avec S40, `54747c8`, déjà doublement audité et clos) : la garde de capacité n'était vérifiée qu'**après** la fin de la chaîne de candidats d'une cellule, pas dans la condition de boucle elle-même — une cellule contenant plus de candidats que la capacité restante du buffer pouvait écrire au-delà et lever `IndexOutOfRangeException`, contredisant directement le contrat documenté (« ne lève jamais pour un buffer sous-dimensionné »). Trouvé indépendamment par les deux auditeurs, par la même méthode (comparaison avec la garde correcte d'`OverlapSphere`) ; corrigé en miroir + test de régression. Findings 🟠 corrigés : l'affirmation « un candidat n'est testé qu'une fois par construction » était vraie pour les coordonnées de cellule mais pas pour le hash de `QueryCellHash` (deux coordonnées distinctes peuvent aliaser sur le même bucket) — dédup `_qVisitedStamp` restauré · un `center` non-fini (NaN/±Infinity) produisait silencieusement `0`, indiscernable d'un vrai miss — nouveau `ValidateCenter`, mirroring `ValidateDirection` · le seuil de chevauchement était sommé en `float` avant comparaison à la distance exacte en `double` — même classe de défaut déjà corrigée une fois sur `RaycastHit.Point` en S40 — élargi en `double` · le tri à distance égale d'`OverlapSphere` **et** `RaycastAll` dépendait de l'ordre non déterministe du parcours de grille/archétype Arch (`Span<T>.Sort` est un introsort instable) — même classe de bug déjà fermée une fois pour `TryRaycast` en S40 mais jamais reportée sur les jumeaux basés sur le tri — clé secondaire `GlobalId` ajoutée aux deux comparateurs. 6 nouveaux tests de régression. **Dette laissée** (🟠/🟡 acceptée, documentée) : `OverlapHit` ne porte ni centre ni rayon, donc aucun moyen pour un appelant de dériver une direction ou une distance de surface — probablement ce qui forcera un changement de forme au premier vrai consommateur, mais D2 était une décision de spec déjà approuvée et aucun consommateur n'existe encore · la racine carrée de `Double3.Distance` est payée sur chaque candidat rejeté, où un pré-filtre en distance au carré serait moins cher (micro-optimisation) · le moteur porte désormais deux chemins de query de région évoluant indépendamment (`OverlapSphere` ici, `QuerySurfaceContacts` pour le défi d'atterrissage) — signalé, pas fusionné (besoins réellement différents aujourd'hui).

**Queries de formes — box overlap, AABB (session 42, clos)** : troisième item §4quater après les queries physiques (S40) et sphere overlap (S41). Spec `docs/plans/2026-09-14-shape-queries-box-overlap-design.md`, approuvée **4,6/5** après 1 seul tour de revue (chaque citation de code réutilisé vérifiée contre le code réel, aucune fabrication trouvée). **`GameWorld.OverlapBox`** (nouveau, `GameWorld.Queries.cs`) — « qu'y a-t-il dans cette boîte ? », **AABB seule** (D1 — pas de rotation, `Double3` n'a ni dot/cross product ni rotation quaternion, une OBB aurait exigé de les ajouter de zéro sans cas d'usage concret). Réutilise **intégralement, sans modification**, `GatherCandidates`/`BuildGrid` de S40/S41 (confirmé par `git diff` : 0 ligne touchée — le pari D4 du raycast tient désormais pour un deuxième consommateur de forme différente). Réutilise aussi `OverlapHit(EntityRef, double Distance)` sans nouveau type (D2, `Distance` = centre-boîte `(min+max)/2` → centre-candidat). Démo `Key.H` (Sandbox+TopDown, même câblage que `Key.F`/`Key.G`). **937 tests**, 0 warning, 0 régression, les 9 captures pinnées re-vérifiées byte-identiques (toutes tombent exactement sur les hashes déjà connus de S40/S41), JIT == NativeAOT confirmé avant/après audit, verdict visuel humain PASS.

Double audit `csharp-lowlevel` (**3,7/5**) + `engine-architect` (**4,1/5**), tous deux PASS-with-concerns — **convergence exceptionnellement forte** : les deux ont prouvé **par mutation**, indépendamment, le même défaut sérieux. **1 🔴 trouvé-et-corrigé** : l'intégralité du chemin grid-walk (marge d'une cellule, dédup par stamp, garde de capacité dans la boucle) n'était exercée par **aucun** des 14 tests livrés — l'estimation minimale du coût de balayage vaut toujours ≥27 cellules (même pour une boîte-point dégénérée), donc avec 1-3 candidats par test, `approxCells >= count` était systématiquement vrai et chaque test tombait silencieusement dans le repli scan linéaire. Le test-vitrine « marge de bordure » du jalon ne prouvait rien : en supprimant la marge, les deux auditeurs ont confirmé que les 14 tests restaient verts. **Le même défaut a été trouvé, indépendamment, déjà présent en silence dans les tests d'`OverlapSphere` de S41** — le repli de coût ajouté par l'audit de S41 avait le même effet sur son propre test de marge, non détecté pendant tout un jalon jusqu'à cette comparaison inter-jalons. Corrigé aux deux endroits en peuplant le monde de test avec assez de leurres lointains (même rayon, donc `cellSize` inchangé) pour forcer le chemin grid — **re-vérifié personnellement par mutation** (neutralisation temporaire de la marge dans le code source des deux méthodes, confirmation que les deux tests corrigés échouent bien, puis restauration). Nouveau test `OverlapBox_GridPath_ManyCandidatesInOneCell_TruncatesWithoutThrowing` ajouté pour couvrir la garde de capacité + le dédup ensemble, également vérifié par mutation (suppression temporaire de la garde → reproduit exactement l'`IndexOutOfRangeException` que la garde existe pour prévenir). Findings 🟠/🟡 corrigés : les tests « boîte/rayon énorme sur monde épars » utilisaient un seul candidat à l'origine, donc le clamp contre l'AABB occupée (sans rapport avec le correctif testé) suffisait déjà à rendre la query bon marché — réécrits avec des candidats vraiment dispersés · le test d'inversion `min > max` ne couvrait que l'axe X — Y et Z ajoutés · `ValidateBox` porte désormais `paramName` · ordre du paramètre `out` de `TryOverlapBox` normalisé · commentaire de `OverlapHit` rendu neutre vis-à-vis de la forme (il ne décrivait que la sphère). **Dette versée au backlog** : `TryRaycast`/`RaycastAll` n'ont aucun repli de coût analogue — leur coût est également dicté par `cellSize` (le plus gros objet du monde), pas par `maxDistance` de la query, et la démo `Key.F` peut déjà atteindre des millions de sondes de dictionnaire par pression dans un monde dense — réel, pré-existant, hors scope de ce jalon.

**Job system, fondations (Job-1) (session 43, clos)** : premier des **N sous-jalons gatés** d'un job system complet (décomposé comme MP-0/Contenu-3c, ampleur comparable à MP-0 entier) — item §4quater après la clôture des 3 queries physiques/formes. Spec `docs/plans/2026-09-15-job-system-foundations-design.md`, approuvée **4,2/5** après 2 tours (round 1 : 3,3/5 NEEDS WORK — chemin de fichier fabriqué pour `PhysicsSystem`, corrigé ; préoccupation de scope légitime — `Stage.Simulation` n'a qu'**un seul système réel** dans tout le dépôt (`PhysicsSystem`), donc ce jalon livre une infrastructure sans charge de travail parallèle vivante aujourd'hui — argumenté et accepté en round 2 comme « infra devançant la charge », précédent MP-0 vérifié authentique). **Portée strictement `Stage.Simulation`** — `Stage.Input`/`PostSimulation`/`Render` restent entièrement séquentiels. `ISystem` gagne trois membres à implémentation par défaut (aucun implémenteur existant ne change) : `Reads`/`Writes` (vides par défaut) et `RequiresExclusiveExecution` (**`true` par défaut** — D6, un système non audité reste aussi séquentiel qu'avant). `SystemScheduler` calcule, une fois au premier `Tick`, un regroupement en **vagues** par conflit glouton (`Writes∩Writes`, `Writes∩Reads`, `Reads∩Writes` = conflit ; `Reads∩Reads` seul jamais) ; tout système `RequiresExclusiveExecution` obtient sa propre vague solo. Exécution réelle sur un **pool de workers persistant, créé paresseusement** (seulement si une vague a effectivement besoin de 2+ systèmes concurrents — raffinement assumé sur la lettre de la spec, qui prévoyait une création systématique : éviter de faire fuir `ProcessorCount-1` threads pour chaque instance de `SystemScheduler`, y compris les centaines qu'une suite de tests en construit), signalisation bas niveau (`SemaphoreSlim`+`CountdownEvent`, jamais `Task.Run`/`Parallel.Invoke` qui alloueraient par appel). `PhysicsSystem.RequiresExclusiveExecution => true` (scratch de broadphase partagé). `GameWorld.AssertOwnerThread`/`SimCommandQueue.AssertOwnerThread` gagnent une liste blanche de threads sanctionnés (D2), peuplée une fois par le pool à sa création.

**966 tests** (+29), 0 warning, 0 régression, les 9 captures pinnées re-vérifiées byte-identiques par preuve A/B `git stash` contre `abdd1d5` (ce jalon ne touche que `Agapanthe.Engine`/`Agapanthe.World`, aucune surface de rendu), JIT == NativeAOT confirmé sur Sandbox/TopDown/HeadlessSim (défaut **`dbe9ed91…`** et `--drive` **`1c760d7f…`** inchangés) avant et après la passe de correctifs d'audit.

Double audit `csharp-lowlevel` (**3,4/5**) + `engine-architect` (**3,5/5**), tous deux PASS-with-concerns — **le changement le plus risqué de l'histoire du projet** (premier vrai multi-threading). **1 défaut bloquant trouvé indépendamment par les deux, sous deux angles complémentaires** : la liste blanche de threads sanctionnés s'appliquait à **la totalité des 25 sites d'appel** d'`AssertOwnerThread` dans `GameWorld`, y compris chaque mutateur structurel (`Spawn`/`Despawn`/`Save`/`Load`/`StepPhysics`/les queries de broadphase) — aucun d'eux n'étant sûr pour un accès concurrent — et **le test livré épinglait le cas dangereux comme la garantie voulue** (`Spawn` depuis un worker ne levant pas), tandis qu'un test séparé faisait effectivement deux appels concurrents `SimCommandQueue.Enqueue` sur la même queue, une vraie race reproduite en aveugle. Corrigé en scindant le garde en deux : `AssertOwnerThread` (honore la liste blanche, réservé aux surfaces **en lecture seule** — seul `IsAlive` l'utilise désormais) et `AssertOwnerThreadStrict` (propriétaire uniquement, jamais la liste blanche — les 24 autres sites, y compris tous les mutateurs et toutes les queries de broadphase partagée) ; les tests concernés réécrits pour épingler l'inverse (`Spawn` depuis un thread sanctionné **lève toujours**). Findings 🟠 corrigés : `SystemScheduler` ne possédait aucun `IDisposable` — chaque pool de workers survivait indéfiniment (threads + primitives de synchro non disposées), un vrai leak contre le principe verrouillé « IDisposable partout » — `Dispose()` ajouté (réveille, joint, dispose), `SimulationHost` aussi ; **découvert en vérifiant le correctif** : aucun test ne disposait son scheduler, ce qui polluait un test non lié (`CopySyncStateTests`) par churn de threads — prouvé par A/B (23/23 propre sur la baseline pré-Job-1, flaky occasionnel avec Job-1 présent), largement résorbé en ajoutant `using` partout · `SetSanctionedWorkerThreads` remplaçait plutôt que d'unir un ensemble déjà sanctionné (deux pools indépendants se seraient dé-sanctionnés l'un l'autre) — corrigé en union + publication `Volatile` · aucun test n'exerçait le chemin d'exception (un système qui lève sur un thread worker) ni le partitionnement à charge inégale — 4 tests ajoutés, vérifiés par mutation · taille du pool de workers plafonnée à la largeur de vague réelle plutôt qu'un `ProcessorCount-1` fixe · `WorkerLoop` enveloppé d'un catch extérieur (un worker qui meurt silencieusement bloquait le thread propriétaire indéfiniment, sans diagnostic) + timeout de 30s en Debug uniquement. **Dette assumée, versée au backlog** : l'utilisation CPU pendant une vague laisse le thread propriétaire inactif au lieu d'exécuter une partition lui-même (perf, pas correctness) · les allocations sur thread worker restent invisibles au compteur 0-alloc par-frame de `SimulationHost` (per-thread, sans conséquence tant qu'aucun vrai système `Stage.Simulation` non-exclusif n'existe) · un résidu de flakiness bas (~1/10-15) sur `CopySyncStateTests` lié au churn de vrais threads OS pendant les tests, non éliminé, sans impact production (le chemin parallèle n'est atteint par aucun hôte réel aujourd'hui) · **sous-jalon 2** : modélisation fine des ressources partagées (scratch physique/query, `SimCommandQueue`) · **sous-jalon 3** : filet de vérification runtime déclaré-vs-réel.

**Synchronization validation (session 25)** : activée par défaut en Debug (`VK_EXT_validation_features` — fournie par la COUCHE, donc invisible à une énumération d'extensions sans nom de couche), désactivable par `AGAPANTHE_SYNC_VALIDATION=0`. Elle a immédiatement révélé **3 hazards préexistants dans la boucle de frame**, invisibles depuis le début du projet : (1) la transition d'acquisition au stage `TOP_OF_PIPE` s'exécutait **avant** l'attente du sémaphore `imageAvailable` (posé à `COLOR_ATTACHMENT_OUTPUT`) → `WRITE_AFTER_READ` contre `vkAcquireNextImageKHR` ; (2) `renderFinished` signalé à `COLOR_ATTACHMENT_OUTPUT` alors que la transition vers `PresentSrc` s'achève à `BOTTOM_OF_PIPE` → `PRESENT_AFTER_WRITE` ; (3) `Undefined` comme source ne posait que le **stage**, pas l'**access** → dépendance d'exécution sans dépendance mémoire, `WRITE_AFTER_WRITE` sur le depth partagé (l'intention était dans le commentaire du code, la barrière était à moitié construite). Corrigés ; **captures `12638edd` et `6e14b23e` inchangées** (la correction ne change aucun pixel). **Le gate « 0 message de validation » couvre désormais les hazards de synchro.**

**MP-0a (session 26, clos) — headless split : la simulation tourne sans GPU** : premier des **4 sous-jalons** de MP-0, qui a été **DÉCOMPOSÉ** (les 4 points du backlog ne partagent ni fichiers ni risques ; un double audit sur 4 sous-systèmes hétérogènes ne vaut rien). Spec `docs/plans/2026-08-13-mp0a-headless-split-design.md`, approuvée **4,15/5** après 2 tours (v1 à 2,85 « Major Gaps » : elle avait bâti son gate central sur un **commentaire** au lieu du code exécutable — `CollectRenderLists` fait un `Set`, pas un `Add`, donc aucun churn d'archétype). **L'ordre exécuté inverse la numérotation du backlog**, qui classe par *sévérité* : le coût du split est **strictement croissant** avec la taille d'`Engine`, alors que les deux 🔴 identité ne sont pas cassés aujourd'hui et ne le deviennent **qu'au moment du partitionnement** (bug et correctif arrivent ensemble). **`Agapanthe.Engine` = simulation headless** (`Stage`, `TickContext`, `ISystem`, `SystemScheduler`, **`SimulationHost`**, `FrameStats`, `PhysicsSystem`, `LandingChallengeRule`) ; **`Agapanthe.Engine.Render`** prend le rendu (`RenderContext`, `IRenderSystem`, `RenderSystemScheduler`, `FrameOrchestrator`, `UiRenderSystem`, `DebugOverlaySystem`). **Le découpage suit les DÉPENDANCES, pas les interfaces** (`DebugOverlaySystem` est un `ISystem` et part côté rendu ; `UiRenderSystem` implémente les deux). `FrameOrchestrator` **compose** un `SimulationHost` et le **reçoit** en surcharge (une simulation existe ; un client y attache une présentation, jamais l'inverse). `RenderSystemScheduler` prend la barrière **directement** → aucun `InternalsVisibleTo` entre les deux assemblies. **`SimulationHost.BeginFrame()` est séparé de `Tick()`** : sous l'accumulator de MP-0c, N ticks partagent une frame et le bracket ouvert dans `Tick` aurait filé **N échantillons par frame** — le gate 0-alloc affiché en continu aurait cessé de mesurer une frame. **`AggregateBoundsSystem` + `_sceneBounds` supprimés** : écrits chaque frame, lus par personne depuis P3-M5 ; un commentaire périmé les a fait survivre 11 sessions. **`samples/HeadlessSim`** (NativeAOT, 1,68 Mo, exit 0, 0 B/frame, snapshot **JIT == AOT** `7c889fec`) est l'artefact de tête. **6 gates d'architecture** : allowlist **statique** sur le `.csproj` (Engine/World/HeadlessSim), « aucun `PackageReference` », closure d'assemblies, surface publique. **Le statique et la closure ne sont PAS redondants, c'est prouvé par mutation** : ré-ajouter `ProjectReference Rendering` sans utiliser de type → statique FAIL, closure VERT (le compilateur élide la référence inutilisée). 491 tests, 0 warning, captures `12638edd`/`03421357` inchangées, 0 leak, 0 validation. Double audit PASS-with-concerns ×2 (4,2/5), aucun 🔴. **Restent MP-0c/d.**

**MP-0b (session 27, clos) — identité d'entité : `GlobalId` 64 bits partitionné + clé de contact physique corrigée** : deuxième des 4 sous-jalons de MP-0. Spec `docs/plans/2026-08-13-mp0b-entity-identity-design.md`, approuvée **4,4/5** après 2 tours. **4 vagues, chacune gardant les captures `12638edd`/`03421357` inchangées jusqu'à la fin** : **W1** `ContactPairKey` (struct 128 bits comparable, `IComparable<T>` contraint jamais boxé) remplace le packing 32 bits `(gid_j<<32)|(uint)gid_k` qui collisionnait silencieusement dès que les ids cessaient d'être denses. **W2** `GlobalIdRange` (`[Start, EndExclusive)`, défaut `[1, ulong.MaxValue)` bit-pour-bit l'ancien compteur) — les 7 sites `_nextGlobalId++` collapsent dans un `NextId()` qui lève bruyamment à l'épuisement. **W3** `UniverseId` (identité **par snapshot**, deux `ulong` — pas un `Guid`, mixed-endian — défaut vide et **non aléatoire** pour préserver le déterminisme JIT-vs-AOT) + snapshot **v2** (en-tête 40 octets, v1 refusé, réconciliation d'univers à **5 cas** testés : both `None` inchangé · adopte · garde · confirme · **throw** sur conflit). **W4 — double audit (`csharp-lowlevel` + `engine-architect`) a trouvé et fait corriger un 🔴** : `Load` avec la 1ʳᵉ version de son paramètre d'allocateur (`GlobalIdRange?`) pouvait laisser un monde recouvrir des ids déjà chargés — les trois points de matérialisation écrivaient `_live[globalId] = entity` par **indexeur**, un spawn ultérieur réémettant le même id **écrasait silencieusement** l'entrée (entité chargée toujours simulée mais indespawnable, disparue du `Save` suivant). Corrigé par un `RegisterLive` unique (`TryAdd` + exception nommée). L'audit architecture a aussi fait remplacer `allocatorOverride: GlobalIdRange?` (qui laissait un bail se réassigner par un paramètre de désérialisation) par **`SnapshotAllocatorPolicy`** (`AdoptFromHeader`/`KeepMine`, sans donnée — le bail reste un fait fixé à la construction) ; `Load` retourne désormais **`SnapshotLoadResult`** au lieu de `void` (la mitigation « adoption loggée » promise par la spec, tenue sans dépendance de logging dans `Agapanthe.World` — l'hôte décide). **`HeadlessSim` re-épinglé** : `7c889fec…` (v1, 1852 o) → **`7e8dc68f5a25914c84677a7a53ad3a58`** (v2, 1868 o, delta = les 16 octets d'`UniverseId`), vérifié JIT==NativeAOT avant et après les corrections W4 ; **le gate vit désormais dans un test** (`HeadlessSimSnapshotFormatTests.cs`) et non plus seulement dans la prose — exactement le trou que la redécouverte de `7c889fec` en session 26 avait signalé. 530 tests, 0 warning, captures inchangées (vérifiées à chaque vague), 0 leak, 0 validation. Double audit PASS-with-concerns (architecture 4,1/5) / 🔴 trouvé-et-corrigé (bas niveau), verdict humain PASS. **Dette laissée** : 🟠 aucun hôte de production ne stampe encore d'`UniverseId` (Sandbox/HeadlessSim chargent en `UniverseId.None` — le **mécanisme** du 🔴 « deux mondes inmergeables » est fermé, son **usage** attend le premier hôte réel `Agapanthe.App`/MP-0c-d) · 🟡 pas de renouvellement de bail avant épuisement de `GlobalIdRange` (additif, aucune refonte requise) · fusion de deux univers rendue **détectable**, pas **implémentée** (hors scope assumé). **Restent MP-0c/d.**

**MP-0c (session 28, clos) — autorité du temps : le tick de simulation découplé de la frame de rendu** : troisième des 4 sous-jalons de MP-0. Spec `docs/plans/2026-09-05-mp0c-time-authority-design.md`, approuvée **4,4/5** après 2 tours (v1 à 3,7 NEEDS REVISION — le test d'équivalence échouait en `float` : `3f/60f ≠ 3f*(1f/60f)` d'1 ULP → 59 vs 60 ticks ; « les hashes de capture vont changer » était **faux** — aucun code prod ne lit `ctx.DeltaSeconds`, mode capture = 1 tick/frame). **`FixedTimestepAccumulator`** (`Agapanthe.Engine`, décision 7 : ni dans `FrameOrchestrator`, ni fusionné dans `SimulationHost`) accumule le dt wall-clock et pilote `SimulationHost.Tick` **N pas fixes** (`Advance` → nombre de ticks ; `AdvanceFrame` = `BeginFrame` + `Advance`, la séquence d'un hôte real-time). **Clamp d'entrée 250 ms** (anti-spiral-of-death) + garde de ratio ctor (`max/fixed > 1024` → throw : sinon `fixed≈1e-9` → boucle infinie, soustraction float = no-op). **Non-fini/négatif → `0f`** (perd la frame, échec inerte — jamais 15 ticks physique en silence) + `SanitisedInputCount` (signal Release, pas un `Debug.Assert` qui tuerait le test host). **`Sanitise` extrait** (patron R3, `float.IsFinite` **avant** `MathF.Min` qui propage NaN). **Déterminisme des captures** (décision 2, prédiction du brainstorm **corrigée** — pas une des 8 décisions) : `AGAPANTHE_MAX_FRAMES` (> 0) → dt synthétique constant = 1 tick/frame ; les hashes **doivent rester inchangés** — HDR `12638edd`, UI `03421357`, prouvé byte-identique sur 3 runs. **`FrameIndex` → `TickIndex`** partout (dette réservée par MP-0a) ; **off-by-one de `CurrentTick` corrigé** : `Math.Max(0L, TickIndex - 1)` = le **dernier tick réellement exécuté** (avant, un système render voyait `N+1`), épinglé par un test pour la première fois. **`PhysicsSystem.RatesMatch(float,float)`** extrait (R3) + `Debug.Assert` ; `PhysicsSettings.FixedDt` inchangé (décision 3). `SimulationHost.LastFrameTickCount` (latché en `EndFrame`, même cycle de vie que `LastFrameMs`) + loggé au banc Sandbox. **`AccumulatorEquivalenceTests`** : **20×`Advance(3·Fixed)` == 60×`Advance(1·Fixed)` == 60 ticks** (assertion **primaire = entiers** ; positions = garde de câblage car `PhysicsSystem` ignore `DeltaSeconds`) + catch-up `FrameOrchestrator`-shape via `AdvanceFrame`. **`AotComponentProbe`** exerce l'accumulateur (`AotAccumulatorSmoke: 1 + 3 ticks`). 558 tests, 0 warning, captures inchangées, `HeadlessSim` `7e8dc68f…` **JIT == AOT** inchangé, 0 leak / 0 validation. Double audit PASS-with-concerns ×2 (`engine-architect` 4,2/5), **aucun 🔴** — 8 findings 🟠/🟡 appliqués. **Dette laissée** (board §Deferred Work) : interpolation visuelle (l'accumulateur détient déjà le facteur de blend `_accumulated/FixedDeltaSeconds`) · `FixedTimestepAccumulator.Reset()` (Load in-process, pause) · **pas fixe = 4 littéraux `1f/60f` indépendants, prérequis netcode** de le rendre une donnée de config de la simulation · `bool HasTicked` (désambiguïse `TickIndex == 0`) · ancrage thread dans `Tick`. **Reste MP-0d.**

**MP-0d (session 29, clos) — input → commandes horodatées : l'input n'atteint plus la sim qu'en tant que commande** : **dernier des 4 sous-jalons de MP-0** (MP-0 CLOS). Spec `docs/plans/2026-09-06-mp0d-input-commands-design.md`, approuvée **4,30/5** (v1 3,33 NEEDS WORK — 2 🔴 + 6 🟠, toutes citations exactes). 4 vagues. **`Agapanthe.Engine` (closure `{Core, World}` intacte)** : `SimCommand` (`readonly record struct`, `[Sequential]`, **56 o**, `TargetTick`/`Kind` **byte opaque**/`Target`/`Double3 Vector`/`Scalar`/`Flags` — offsets épinglés) · `InputSnapshot` (`[Sequential]`, **40 o**, `Held`/`Pressed`/`Released` + `InputAxes` = `[InlineArray(4)] float`, **1ʳᵉ utilisation projet**) · `InputMap` déclaratif (`BindButton(bit,kind,OnPress|OnRelease|WhileHeld)` + `BindAxisVector`) · `SimCommandQueue` (insertion sort stable = FIFO à tick égal, `DrainUpTo(tick)` **lève le préfixe dû dans un scratch et compacte AVANT d'appeler les handlers** → un handler peut `Enqueue` sans corruption, sa commande attend le drain suivant ; drain ré-entrant → throw ; garde owner-thread `[Conditional("DEBUG")]` qui **lève**, patron `GameWorld.AssertOwnerThread`) · `InputTranslation.Emit` (static, stateless, 0-alloc, pas de `prev` — edges dans `Pressed`/`Released`). **`SimulationHost`** : phase input dans `Tick` **avant `Stage.Input`** (`SampleInput` → `InputTranslation.Emit` → `Commands.DrainUpTo(_scheduler.TickIndex)`), drain avant toute stage → `ApplyCommand` mute sans query en vol, `StepPhysics` voit la vélocité le même tick ; `ApplyCommand` null → `DiscardedCommandCount` (signal Release, patron `SanitisedInputCount` de MP-0c — **pas** un `Debug.Assert`). **`GameWorld.SetBodyVelocity(EntityRef, Vector3)`** : gardes standard **+ `Has<Velocity>()` → throw** (Arch `Set<T>` sur le mauvais archétype = écriture hors-bornes silencieuse, et `SimCommand.Target` est une donnée externe — audit LL 🔴). Le binding axis déclaratif laisse **`Target = default`** : un client n'a pas autorité sur *quelle* entité — le routing d'ownership vivra dans `ApplyCommand`, jamais dans la map (`EntityRef` ctor `internal` l'impose). **Démos** : `HeadlessSim --drive` (séquence `InputSnapshot` scriptée, **le gate déterministe**, MD5 `97e786f0455a53d856b9ba4affca1003` / 208 o, **JIT == AOT**, épinglé dans `HeadlessSimSnapshotFormatTests`) · Sandbox `AGAPANTHE_SCENE=drive` (interactif, **non épinglé**, caméra fixe, `X` = brake) · `Key.B` (planet-drop/challenge) → `orchestrator.Simulation.Commands.Enqueue` stampé `TickIndex` portant `camera.Position`, routé par `ApplyCommand` vers `TryShoot`/`DropOne`. **589 tests** (+31), 0 warning, captures HDR `12638edd…` / UI `03421357…` **inchangées** ×3, `HeadlessSim` défaut `7e8dc68f…` inchangé, `AotComponentProbe` PASS (`IsDynamicCodeSupported=False`), 0 leak / 0 validation. Double audit : `engine-architect` PASS-with-concerns **4,3/5** (aucun 🔴), `csharp-lowlevel` PASS-with-concerns (**1 🔴 trouvé-et-corrigé** — `SetBodyVelocity` sans `Has<Velocity>`). **Dette laissée** (board §Deferred) : extraire `SimulationInput` de `SimulationHost` **au 2ᵉ concern** rejoignant le groupe input (receive réseau / ownership / replay log), pas avant · purge `Commands` sur un `GameWorld.Load` in-process (avec la dette `FixedTimestepAccumulator.Reset()`) · cap anti-flood de `SimCommandQueue` · `OriginatorId` / identité de pair · le clone `RunDrive` dans `HeadlessSimSnapshotFormatTests`.

**Agapanthe.App (session 30, clos) — premier hôte de production : `Program.cs` (2360 l) → `AppHost` + contrat `Game`** : premier item d'après MP-0 (backlog §4quater). Spec `docs/plans/2026-09-07-agapanthe-app-design.md`, approuvée **4,40/5** après **3 tours** de revue scorée (v1 3,13 — référence de projet circulaire `App↔Platform` ; v2 3,88 — couverture de test factice ; v3 4,40). **Nouveau `src/Agapanthe.App`** : `IWindow` (abstrait le backend fenêtre/boucle, expose `Silk.NET.Input.Key`), `IGame` (Title, Scenes, DefaultScene, `Universe => UniverseId.None` DIM), `ISceneRecipe.Build(SceneContext)`, `HostOptions.FromEnvironment(Func<string,string?>?)`, **`AppHost.RunClient(IGame, IWindow, string[], HostOptions?)`** — possède le bootstrap GPU + la frame loop fixed-step + le harness capture + le **teardown ordre-strict M4-11 (le gate 0-leak)**, porté verbatim. **`App` NE référence PAS `Platform`** (sinon Vulkan entre transitivement dans un leaf) : `EngineWindowAdapter : IWindow` vit dans `samples/Sandbox` — `EngineIsHeadlessTests` a une `[InlineData]` sur `App.csproj` (allowlist statique, patron MP-0a). **Couture composition-root de MP-0a enfin exercée** : `SimulationHost.CreateDefault(world, SimulationSettings.Default)` puis `FrameOrchestrator.CreateDefault(sim, world, …)` (la surcharge « attache une présentation »). **`SimulationSettings { FixedDeltaSeconds }` sur `SimulationHost` = définition unique du pas fixe** (prérequis netcode) : `FrameOrchestrator.ResolveAccumulatorStep` le lit, les recipes threadent `ctx.Simulation.Settings.FixedDeltaSeconds` dans leur `PhysicsSettings`, `HeadlessSim` supprime son `const FixedDt` ; `PhysicsSettings` (dans `World`, ne voit pas `Engine`) garde son littéral, réconcilié runtime par `PhysicsSystem.RatesMatch`. **Dette MP-0b payée** : `AppHost.ResolveUniverse(game, options)` = `options.Universe ?? game.Universe` → `new GameWorld(GlobalIdRange.Default, resolved)` — premier hôte de prod qui stampe un `UniverseId` ; `SandboxGame` reste `None` → tous les hashs inchangés ; `AGAPANTHE_UNIVERSE` override. **Sandbox thin** : `Program.cs` **2360 → 22 lignes** ; **5 recipes** (`ModelSceneRecipe` couvre `model`/`grid:`/`drop:` — un seul chemin ; `Planet`/`PlanetDrop`/`PlanetChallenge` partagent `PlanetStage` ; `Drive`) + `Content/` + `Cameras/` + `Systems/` + `Tools/IblTestTool`. Caméra free-fly : les recipes s'abonnent à `ctx.Window.Updated` (déplacement verbatim du handler, pas vers `SampleInput`) via `RecipeInput.WireFreeFly` ; `drive` = caméra fixe. Clavier : host garde Escape/sensibilité/exposition/N/L/F3 ; `B`/`X`/`F5` → recipes. **614 tests** (+25), 0 warning, captures HDR `12638edd…` / UI `03421357…` **inchangées** ×3, `model` baseline `df55d444…`, `HeadlessSim` `7e8dc68f…` (1868 o) + `--drive` `97e786f0…` **inchangés**, **Sandbox + HeadlessSim JIT == NativeAOT**, `AotComponentProbe` PASS, 0 leak / 0 validation. Double audit : `engine-architect` PASS-with-concerns **4,2/5** (**1 🔴 trouvé-et-corrigé** — `RunClient` renvoyait 0 sur échec d'init, `clean` écrasé par l'étape Report du teardown ; corrigé par un flag `failed` + `return clean && !failed`), `csharp-lowlevel` PASS-with-concerns **aucun 🔴** (teardown ordre EXACT, hot path 0-alloc, extractions fidèles). **Corrigé post-audit (W5)** : 🔴 exit code + isolation par étape du teardown · `[InlineData]` `App↛Platform` · `Log.Warn` `AGAPANTHE_LOAD` non consommé · **`HostOptions.Scene`/`SavePath`/`VerifyCull`/`ShaderReloadTest`** (plus aucune lecture d'env dans `RunClient`) · surface morte `IWindow` retirée · garde de longueur `Ppm` · `IWindowSurfaceTests` (équivalent du test 5) · dédups. **Dette laissée** (board §Deferred, S30) : 🟠 **scission `SceneContext` sim/présentation** (tout est `required` → une recipe ne se construit pas headless, `HeadlessSim` partage 0 code de peuplement — **mord à la 2ᵉ slice + `RunDedicatedServer`**) · 🟠 `EngineWindowAdapter` (~75 l) → projet dédié à l'app n°2 · 🟠 helpers caméra/lumière/sol/sky → `App` au jalon contenu · 🟡 exit code modèle-introuvable 2→1 · 🟡 `FrameOrchestrator` param médian retiré (note API) · 🟡 ordre de chargement d'assets planet changé (un `.save` pré-refactor peut ne plus résoudre). **Verdict visuel humain : DÛ.**

**Contenu-1 (session 31, clos) — identité d'assets stable** : premier des **3 sous-jalons** du domaine **Contenu** (décomposé comme MP-0 : 1. identité d'assets stable · 2. cook offline + manifest · 3. prefabs/scènes déclaratifs — feu vert humain entre chaque). Spec `docs/plans/2026-09-07-content-asset-identity-design.md`, approuvée **4,60/5** (2 tours). Donne à chaque render asset une identité **path-based stable** (`AssetKey` — `readonly record struct` dans `Core`, style Godot `res://`, `AssetKey("models/DamagedHelmet.glb")` / `AssetKey("sandbox/planet-surface")` pour un procédural nommé par l'appelant ; `None => default`, `Value` null ssi None ; **la casse compte**). Snapshot du World **v2 → v3** : `MeshRef` passe de 2 handles bruts blittables (slots process-locaux de `ResourceRegistry`) à `(clé, index mesh local, index material local)`, re-résolu au load via un délégué `MeshRefResolver` fourni par l'hôte. **Ferme la dette VS-1 « Option 2 »** — le contrat « Option 1 » exigeait de recharger les assets dans le MÊME ORDRE ou le snapshot restaurait un drawable silencieusement faux (aggravé par Agapanthe.App S30). **`Agapanthe.World` ne référence toujours PAS `Rendering`** : les délégués `MeshRefIdentifier`/`MeshRefResolver` ne nomment que des types `Core` (`MeshHandle`/`MaterialHandle`/`AssetKey`), l'hôte branche `registry.IdentifyMeshRef`/`.ResolveMeshRef` (patron `SnapshotAllocatorPolicy`). **`ModelKeyIndex`** (`internal`, `Rendering`, GPU-free — map bidirectionnelle `AssetKey ⇄ MeshHandle[]/MaterialHandle[]`) extrait de `ResourceRegistry` pour être testable sans `GraphicsDevice`. Header 40 o inchangé + key table triée **ordinal** (`string.CompareOrdinal`, déterminisme byte-exact cross-machine, validée strictement croissante à la lecture) ; `MeshRef` 16 o → 12 o ; **v1 ET v2 refusés** avec messages dédiés. Sans résolveur → tout `MeshRef` en `MeshHandle.Invalid` (load headless / `HeadlessSim` inchangé) ; avec résolveur, il **DOIT lever** sur une clé non chargée (jamais un `None` silencieux). **Le mécanisme a trouvé son propre bug en W3** : `PlanetStage.Build` restaurait le snapshot avant que la recipe n'enregistre son beacon → `GraphicsException` bruyante = exactement le bug fermé ; corrigé en scindant `Build` + `RestoreIfRequested` (appelé après enregistrement des assets propres à la recipe). **659 tests** (+ ~17), 0 warning, captures HDR `12638edd…` / UI `03421357…` **inchangées**, `HeadlessSim` re-épinglé **JIT == NativeAOT** défaut `80ced166fdf3076119a62970f63d683b` (1842 o) / `--drive` `cf01492e8a9688b666e01d2ef9d63869` (210 o), `AotComponentProbe` PASS JIT+AOT (exerce désormais le chemin key table), round-trip `planet-challenge`/`planet-drop` rend, 0 leak / 0 validation. Double audit `csharp-lowlevel` + `engine-architect` **PASS-with-concerns 4,3/5 chacun, aucun bloquant** — findings contenus appliqués (couverture AOT du key table, table strictement croissante + UTF-8 strict au load, `Identify(Invalid)` → `None`, `ModelKeyIndex.Add` atomique). **Dette laissée** (board §Deferred + backlog §4quater) : 🟠 **identité d'asset côté simulation** (`MeshRef` est render-local, un `Save` headless écrit `None` → un serveur autoritaire ne peut pas émettre d'état visuellement reconstructible ; un composant sim-side `AssetRef` + `MeshRef` sa projection client = **la décision structurante de Contenu-3**) · 🟠 clés Sandbox dérivées de `Path.GetFileName` (répertoire jeté) → clés relatives à une racine de contenu à Contenu-2 · 🟠 `RestoreIfRequested` = étape obligatoire non gardée (lié à la scission `SceneContext` S30). **Verdict visuel humain : DÛ** (partagé avec Agapanthe.App S30).

**Contenu-2 (session 32, clos) — cook offline + content manifest** : 2ᵉ des 3 sous-jalons du domaine **Contenu**. Spec `docs/plans/2026-09-08-content-asset-cook-design.md`, approuvée **4,24/5** (relecteur séparé, 2 tours : 3,52 → 4,24). Un **`tools/AssetCooker`** (Exe, patron `FontCooker`) transforme chaque `content/**/*.glb` en un blob **`.agmodel`** (autonome : meshes SoA + materials + images RGBA8 décodées, `DeflateStream(Optimal)`, déterministe même-machine, reader `public` / writer `internal` — patron `.agfont`), écrit un **`content.agmanifest`** binaire (`AssetKey → {blob, kind, hash[32]}`, trié ordinal, garde strictement croissant), lu au runtime par un **`AssetCatalog`** (`Open(root)` / `LoadModel(key) → ModelAsset`, GPU-free, sans cache, possédé par `AppHost`, exposé via `SceneContext.Catalog`). **Le runtime ne parse plus glTF** : `GltfLoader` + `Gltf/*` + `AccessorReader` + `TangentGenerator` + `ImageLoader` migrent par `git mv` dans un nouveau **`src/Agapanthe.Assets.Pipeline`** (cook-side, PAS `IsAotCompatible`, tire `StbImageSharp`) ; `Agapanthe.Assets` runtime garde les DTOs + readers `.agfont`/`.agmodel` + `AssetCatalog` + `HdrImageLoader` (+ `StbImageSharp` pour l'HDR — gain AOT partiel assumé). **`AssetsPipelineIsolationTests`** (patron MP-0a) : scan de **tous** les `.csproj` + check réflexif sur l'assembly chargé → `Assets.Pipeline` **et** `tools/AssetCooker` référencés uniquement par le cooker + les tests. Target MSBuild `CookAssets` dans `Sandbox.csproj` (jumeau `CookFonts` : `dotnet exec`, stamp mtime + skip fin content-hash dans `CookRunner`, `RemoveProperties`, tmp-then-move, purge des blobs orphelins). L'arg CLI du Sandbox devient une **clé** (`-- MetalRoughSpheres.glb` → `models/MetalRoughSpheres.glb`) ; plus de chargement glTF par chemin arbitraire. **`AssetKey.FromContentPath`** (Core) solde la dette audit Contenu-1 (clés dérivées de `Path.GetFileName`). **689 tests** (+ ~30), 0 warning, captures **`model` re-épinglée `9030f6a64e9587b05d5abb99b487b1b9`** (la ref S30 `df55d444…` avait déjà dérivé — un `git stash` prouve que le contenu cuit rend à l'octet près comme le chemin remplacé) / `planet-drop` HDR `12638edd…` / UI `03421357…` **inchangées** ; `HeadlessSim` `80ced166…` / `cf01492e…` **inchangés** ; `AotComponentProbe` PASS ; **Sandbox + HeadlessSim JIT == NativeAOT** (`AgModelFormat.Read` + `DeflateStream` inflate + `MemoryMarshal.Cast` tous AOT-clean). Double audit `csharp-lowlevel` **4,1/5** + `engine-architect` **4,2/5** PASS-with-concerns, **aucun bloquant** — findings contenus appliqués (sécurité mémoire du parser : bornes de count avant alloc + plafond 1 GiB ; `CookRunner` globe `.glb` seul — un `.gltf` avec siblings non trackés ne peut pas shipper un blob périmé en silence ; `blobPath` validé comme la clé ; catalog tolérant à un manifest absent pour les scènes procédurales). **Dette laissée** (board §Deferred + backlog §4quater) : 🟠 **Contenu-2b** — `.gltf` + graphe de deps (siblings `.bin`/image), HDR/textures/fonts standalone dans le catalog (`AssetKind.Environment` déjà réservé), décodeur RGBE maison pour sortir `StbImageSharp` du runtime · 🟠 blob = 21 Mo pour DamagedHelmet (RGBA8 décodé) → **séparation géométrie/images groupée avec BCn** (jalon rendu), réversible via `AgModelFormat.Version` · 🟠 les ~170 l de targets de cook dans `Sandbox.csproj` → `build/Agapanthe.Cook.targets` à l'app n°2 · 🟡 `HostOptions.VerifyContentHashes` (le `contentHash[32]` est écrit/shippé, pas encore lu). **Verdict visuel humain : DÛ** (partagé avec Agapanthe.App S30 + Contenu-1 S31).

**Contenu-3a (session 33, clos) — identité d'asset dans la simulation + snapshot v4 + scission `SceneContext`** : 1ᵉʳ des **3 sous-jalons** de Contenu-3 (décomposé en interview S33 : 3a fondation · 3b `.agscene`/`.agprefab` TOML→blob cuit + `SceneLoader` · 3c 3 registres de fabriques + migration des 5 recipes). Spec `docs/plans/2026-09-09-content-3a-sim-asset-identity-design.md`, approuvée **4,55/5** (revue scorée 3,2 → 4,55). **Aucun format, aucun cooker, aucun `SceneLoader`** — pure fondation. **`AssetRef`** = composant **#13**, 1ᵉʳ composant **managé** du projet (porte un `MeshRefKey` = `AssetKey` string + 2 indices locaux ; W0-spike prouve qu'Arch le gère proprement sous NativeAOT — `Root<AssetRef>()` suffit, 0 boxing). Chaque drawable/body le porte, renseigné à la matérialisation depuis `ImportedEntitySpec.Identity` (nouveau champ + param ctor `= default` ; `ResourceRegistry.Load` le renseigne, threadé à la main dans 5 sites de copie de spec). **Snapshot v4** : sérialise `AssetRef` (dans l'ancienne forme 3-u32 de `MeshRef`), **ne sérialise plus `MeshRef`** (cache render dérivé au load via le resolver, comme `InstanceSlot`), **supprime le délégué `MeshRefIdentifier`** → un `Save` headless émet de vraies `AssetKey` sans callback ; **upgrade v3→v4 en place** (`identitySlot = isV3 ? 5 : 12`, tail partagée ; fixture `world-v3.save` capturée avant le bump). `MeshRefResolver` conservé. **`SceneContext` → scission** : `SimSceneContext` (headless-safe : World, Simulation, Catalog, Args, Options, `AddSystem`, `RequestRestore`/`ApplyPendingRestore`) + `PresentationSceneContext` (nullable, GPU/fenêtre). `ISceneRecipe.Build(SimSceneContext, PresentationSceneContext?)` — les 5 recipes ouvrent sur `?? throw`. **Garde-fou restore** (dette Contenu-1) : la recipe *déclare* `RequestRestore(path)` après enregistrement de ses assets ; l'hôte appelle **un** `ApplyPendingRestore(registry.ResolveMeshRef)` après `Build` ; les 3 `PlanetStage.RestoreIfRequested` épars disparaissent. **`HeadlessSim`** re-épinglé **JIT == NativeAOT** avec de vraies clés (audit F4) : défaut `6a13dd54c1db32d35a15332bff0395e7` (1857 o) / `--drive` `f6053226f8c13b55589b29be103a8e66` (231 o) — le binaire headless shippé prouve désormais « un process sans GPU émet une identité d'asset reconstructible ». **696 tests**, 0 warning, captures **inchangées** (`model` `9030f6a6…`, `planet-drop` HDR `bc8440ab…` — `git stash` A/B prouve le no-op rendu ; les valeurs `12638edd`/`03421357` de ce doc = dérive cross-env pré-existante, re-baseline S33), `AotComponentProbe` count 13 PASS, **Sandbox + HeadlessSim JIT == NativeAOT**, 0 leak / 0 validation. Double audit : `csharp-lowlevel` **3,9/5** + `engine-architect` **4,0/5** PASS-with-concerns — **1 🔴 trouvé par les deux et corrigé** : `LandingChallengeSystem` seedait `_shotsIssued` dans son ctor ; D7 ayant déplacé le restore *après* `Build`, une reprise seedait sur un monde vide (budget de tirs remis à neuf) → seed paresseux au 1ᵉʳ tick + test d'ordre. 10 des 12 findings 🟠/🟡 appliqués (stream→path pour `RequestRestore`, gate structurel réflexif `SimSceneContext_NamesNoGpuOrWindowType`, check DEBUG `WarnIfDrawablesMissingIdentity`, probe exerce despawn d'un drawable keyé, `ModelKeyIndex` maps mortes → `HashSet`, …). **Dette laissée** (board §Deferred) : 🟠 coût GC de mark du `AssetRef[]` managé **non mesuré** (`dotnet-counters` absent ; fallback D1-alt = id blittable + side-table) · 🟠 règle « rien dans `Build` ne lit le contenu du monde » à inscrire dans `ISceneRecipe` · 🟠 les 5+ copies champ-à-champ de spec → à supprimer quand `SceneLoader` est le chemin unique (3b) · 🟠 `Agapanthe.App` → `App` + `App.Client` avant un vrai `RunDedicatedServer` (Vulkan dans la closure d'App) · identité d'asset **non-modèle** (textures/env/fonts) toujours hors snapshot. **Verdict visuel humain : PASS** (2026-09-09 ; protocole : `model` + `planet-challenge` rendent, une reprise `F5`→`AGAPANTHE_LOAD` reprend correctement le budget de tirs).

**Contenu-3b (session 34, clos) — `.agscene`/`.agprefab` TOML→blob cuit + `SceneLoader` : client et serveur partagent le peuplement** : 2ᵉ des 3 sous-jalons de Contenu-3 — **Contenu CLOS (3/3)**. Spec `docs/plans/2026-09-09-content-3b-declarative-scenes-design.md`, approuvée **4,4/5** (revue scorée 3,3 → 4,4). Un **format de scène déclaratif** : authoring **TOML** (Tomlyn, cook-side uniquement) cuit hors-ligne en blobs binaires déterministes **`.agscene`** (`AGSC`, patron `.agmodel`/`.agfont` : reader `public` / writer `internal`, `DeflateStream`, `Reader` ref struct bornée), lu au runtime par un **`SceneLoader` GPU-free** (nouveau projet **`Agapanthe.Scene`** = `{Core, World, Assets}`, allowlisté). **Bénéfice central** : `HeadlessSim --scene <clé>` charge exactement le même fichier que le Sandbox — `SceneMaterializer.Materialize` est le code de peuplement unique, `MeshHandle.Invalid` + `AssetRef` côté headless, résolu côté client via **`GameWorld.ResolveMeshRefs(MeshRefResolver)`** (nouveau, réutilise le mécanisme Contenu-3a). La famille `model` (single/grid/cluster) **devient data** (`content/scenes/{model,grid,drop,metalrough}.toml`) ; les prefabs (`content/prefabs/helmet.toml`) sont des **includes cook-time inlinés**, pas un type d'asset runtime. **Expansion au cook-time** : `SceneCompiler.EmitGrid`/`EmitCluster` déroulent les directives `[[grid]]`/`[[cluster]]` en liste plate d'entités (le jitter `Hash(i)` du cube-cluster est copié du code Sandbox pré-existant, puis ce code **supprimé** — 0 appelant restant) ; le runtime n'est qu'une boucle triviale. **`.agmodel` bump v2** : `MeshAsset` gagne `BoundsCenter`/`BoundsRadius` précalculés au cook (`MeshBounds.Compute`, même algorithme que l'ancien calcul GPU-side — capture byte-identique), `SceneBuilder` les lit et garde un fallback pour le procédural (`BoundsRadius == 0`). `ModelSceneRecipe` **supprimée** → `SceneRecipe(string sceneName)` générique, un par scène. Captures **re-épinglées + verdict visuel humain PASS** sur les 4 scènes `model`/`grid`/`drop`/`metalrough` (nouvelle composition studio-HDR + lumière déclarative) ; `planet-drop` inchangée (`bc8440ab…`, dérive cross-env pré-existante documentée). **HeadlessSim** : `--scene headless-default` cuit + charge le même chemin, MD5 `8a5c0463…` **JIT == NativeAOT**, défaut `6a13dd54…` inchangé. **740 tests**, 0 warning, 0 leak / 0 validation, AOT PASS. Double audit `csharp-lowlevel` **4,1/5** + `engine-architect` **4,1/5** PASS-with-concerns, **aucun 🔴** — 12 findings 🟠 appliqués **cette session** : durcissement `ReadCount` des deux formats cuits (`minRecordBytes`, contre un count forgé dimensionnant un tableau de références bien au-delà de ce que le blob compressé pourrait produire) · `ResolveMeshRefs` pose `_structuralDirty` **avant** la boucle (une résolution partielle qui lève ne doit pas laisser le buffer de slots persistants désynchronisé) · validation `LocalMat` dans `SceneMaterializer` (symétrique à `LocalMesh`, un serveur headless n'a pas de resolver pour l'attraper plus tard) · convention mesh-sans-matériau alignée sur `ResourceRegistry` (`Materials.Count`, pas `0`) · rejet NaN sur les bounds v2 · rejet direction-lumière nulle au cook (sinon `Normalize` → NaN silencieux dans le CSM) · `SceneTomlReader` rejette les clés TOML inconnues (promis par la doc, jamais appliqué) · un chemin `Doubles()` dédié pour `world_origin`/positions (l'ancien passait par `float[]`, quantifiant silencieusement une position à grande magnitude) · l'offset d'un membre de prefab est désormais tourné par la transform de l'instance · l'incrémentalité du cook inclut le hash des prefabs référencés · les clés de scène passent par `AssetKey.FromContentPath` (la régression exacte du fix Contenu-1 « répertoire jeté », mais pour les scènes). **Dette laissée** (board §Deferred Work) : 🟠 le câblage `MaterializeResult.Physics → PhysicsSystem` est dupliqué par hôte (`Engine` ne peut nommer `MaterializeResult`, `App` porte Vulkan) — la forme concrète de la décision `App`/`App.Client` à venir · 🟠 `ResourceRegistry.Unload` a désormais 0 appelant (son seul exerciseur, `ModelSceneRecipe`, a disparu) — a besoin d'un chemin d'exercice GPU réel, idéalement au reload de scène de 3c · 🟡 `SceneLoader.LoadHeadless` nommé pour le headless mais appelé aussi par le client · 🟡 `AGAPANTHE_VIEW` lu directement dans `SceneCameraApplier` (brèche mineure de l'invariant S30 « aucune lecture d'env hors `HostOptions` ») · 🟡 collisions de nommage `Scene` (assembly vs `Agapanthe.Assets.Scene` vs `Rendering.SceneLights`). **`ModelContent.ResolveModelKey` + l'arg CLI modèle du Sandbox restent** (déviation D10 documentée — `DriveSceneRecipe` les utilise encore, migre en 3c).

**Contenu-3c-1 (session 35, clos) — registres de fabriques + migration `planet`/`planet-drop`** : 1ᵉʳ des **3 sous-phases gatées** de Contenu-3c (3c-1 infra + `planet`/`planet-drop` · 3c-2 `LandingChallenge` + `planet-challenge` · 3c-3 `ground_quad` + `drive` + cleanup — décidé en interview, ordre inverse de sévérité que MP-0/Contenu car ici le format+registre est un prérequis partagé strict). Spec `docs/plans/2026-09-11-content-3c-scene-systems-design.md`, approuvée **4,30/5** (revue scorée 3,70 → 4,30). **`.agscene` v2** : `ScenePhysics` gagne un attracteur newtonien optionnel (`Mu`/`AttractorCenter`/`SurfaceRadius`, `Mu==0` ⇒ chemin uniforme byte-identique, patron `PhysicsSettings.WithAttractor`) ; `SceneCamera.Fixed` gagne `MoveSpeed`/`ShadowDistance` ; `SceneEnvironmentMode` +`ProceduralSky`/`Black` (sans payload) ; nouveau `[[system]]` — `SceneSystemKind.ProbeDrop` **seul déclaré** (pas de pré-déclaration spéculative de `LandingChallenge`, YAGNI confirmé par les deux audits). **Deux registres, pas trois** (décision verrouillée en interview — une 3ᵉ table « command-handler » a été explicitement rejetée, chaque fabrique câble son propre input) : **`ISceneSystemFactory`** client (`IGame.SceneSystems`, sur **`PresentationSceneContext.SceneSystemFactories`** — déplacé depuis `SimSceneContext` post-audit car `Create` nomme `PresentationSceneContext`, y laisser le registre aurait réintroduit un type GPU/fenêtre dans le contexte headless-safe) et un registre de **générateurs procéduraux cook-time-only** (`Agapanthe.Assets.Pipeline/Procedural/`, invisible à `IGame`/au runtime). **`UvSphereGenerator`** réimplémente `Primitives.UvSphere` en SoA cook-side (vérifié byte-identique par test dédié, le cook-side ne peut pas référencer `Rendering`) → sphères planète/soleil/probe deviennent de vrais blobs `.agmodel` cuits, incrémentalité pure-hash-TOML. **Le plus gros réducteur de scope** : les 3 caméras planète bespoke (`FramePlanetCamera`/`FramePlanetDropCamera`/`FramePlanetChallengeCamera`) calculent une pose entièrement déterministe depuis des constantes de scène → collapsent en trig cuite au cook-time dans `SceneCamera.Fixed`, zéro nouvelle logique caméra runtime. **`SceneMaterializer.BuildRuntimeTemplate`** (nouveau, public, GPU-free) produit un gabarit `ImportedEntitySpec` (handles Invalid) qu'un `ISceneSystemFactory` client complète via `ResourceRegistry.ResolveMeshRef` — permet à un système d'ajouter des entités **runtime** (probes larguées) qui portent la même identité d'asset qu'au chargement. `SceneRecipe.Build` dispatche `def.Systems` après `ClientScenePresenter.Apply`, garde centralement le slot unique `SimulationHost.ApplyCommand` sur toute la boucle de dispatch (hissé, pas dupliqué par fabrique). **`HeadlessSim` refuse (exit 1)** toute scène déclarant des systèmes — concept client-only, jamais un run partiel silencieux (D9). `PlanetSceneRecipe`/`PlanetDropSceneRecipe` supprimées ; `planet`/`planet-drop` deviennent `SceneRecipe("planet"/"planet-drop")` génériques + `content/procedural/{planet-surface,sun-surface,probe}.toml` + `content/scenes/{planet,planet-drop}.toml` (nombres cuits dérivés d'un script jetable rejouant les anciennes formules `SandboxCameras`/`PlanetContent` à leurs défauts env-var — pas de reproduction bit-identique des quirks float de l'ancien code, aucun hash n'était pinné avant migration, le gate réel = nouveau MD5 + verdict visuel). **780 tests**, 0 warning, captures `planet`/`planet-drop` **pinned + verdict visuel humain PASS** (`99e2f4a3…`/`81ddf074…`), `model`/`grid`/`drop`/`metalrough`/`headless-default`/`planet-challenge`/`drive` tous re-vérifiés inchangés, `--scene planet-drop` → exit 1 confirmé (D9), Sandbox + HeadlessSim + `AotComponentProbe` **JIT == NativeAOT** sur toutes captures/snapshots (re-confirmé après corrections d'audit), 0 leak / 0 validation. Double audit `csharp-lowlevel` + `engine-architect` PASS-with-concerns, **1 🔴 trouvé indépendamment par les deux et corrigé** : `SceneMaterializer.Materialize` parsait/compilait/round-trippait l'attracteur mais n'appelait jamais `.WithAttractor(...)` — `planet-drop` tournait à **gravité nulle**, chaque probe restait figée, bug invisible dans la capture pinned (aucune probe n'y apparaît, elle ne spawn que sur touche `B`) — découvert uniquement par l'audit, jamais par le protocole visuel ; corrigé + 2 tests de régression ajoutés (`SceneMaterializerTests` — csharp-lowlevel avait explicitement pointé le trou de couverture `ScenePhysics → PhysicsSettings`, exactement la couture qui a cassé). 4 findings 🟠 appliqués : bounds check `localMat` manquant dans `BuildRuntimeTemplate` (symétrique au check entity-path pré-existant) · garde de suffixe `.toml` dans `CookRunner` (un near-miss de wildcard Win32 pouvait mal-keyer un fichier — nouveau `StripTomlExtension`) · déplacement `SceneSystemFactories` `SimSceneContext`→`PresentationSceneContext` (ci-dessus) · warning `AGAPANTHE_LOAD` pour toute scène cuite sans bloc `[restore]` (vrai depuis Contenu-3b pour toute scène `SceneRecipe`, pas une régression 3c-1 — le §3.5 du spec l'affirmait à tort « inaffecté »). **Dette laissée** (board §Deferred, → 3c-2/3c-3) : validation `ProbeRadius`/`Every` · sémantique sentinelle `MoveSpeed`/`ShadowDistance` (0 = dérivation dynamique, pas un contrat formellement documenté) · fabrique dupliquée sur un même `Kind` prend silencieusement la première trouvée · `ApplyEnvironment` sans bras `default` explicite · script `bake_planet.cs` non committé. **Verdict visuel humain : PASS** (2026-09-12).

**Contenu-3c-2 (session 36, clos) — `LandingChallenge` + migration `planet-challenge` + correctif générique de reprise** : 2ᵉ des **3 sous-phases gatées** de Contenu-3c. Spec `docs/plans/2026-09-11-content-3c-scene-systems-design.md` §9 (ajouté cette session, concrétise ce que 3c-1 avait délibérément différé). **`.agscene` v2→v3** : `SceneSystemKind.LandingChallenge = 1` ; `SceneSystem` gagne `ZoneCenter`/`ZoneRadius`/`SurfaceBand`/`DropHeight`/`TargetCount`/`ShotBudget`/`QuicksavePath` — **`AttractorCenter`/`SurfaceRadius` délibérément PAS dupliqués** (déjà sur `ScenePhysics` depuis 3c-1) : la fabrique les lit sur `MaterializeResult.Physics`, rendant irreprésentable la classe de bug « le zonage de la reprise diverge de la physique qui y fait tomber les probes ». Nouveau `LandingChallengeSystemFactory` (client, `Stage.PostSimulation`), construit la classe **`LandingChallengeSystem` INCHANGÉE** (le seed paresseux au 1ᵉʳ tick, fix 🔴 de Contenu-3a, survit intact — vérifié par `git diff`) ; câble `B`/`F5` via le même `RecipeInput.SpawnProbeCommandKind` que `ProbeDropSystemFactory` (garde single-slot `ApplyCommand` centralisée dans `SceneRecipe` toujours suffisante à 2 fabriques). `PlanetChallengeSceneRecipe`/`PlanetStage`/`PlanetContent.cs` (entièrement mort) /`SandboxCameras.FramePlanetChallengeCamera` supprimés (0 appelant grep-vérifié) ; `planet-challenge` devient `SceneRecipe("planet-challenge")` + `content/procedural/beacon.toml` + `content/scenes/planet-challenge.toml` (nombres cuits via script jetable, réutilisant les valeurs soleil/physique déjà pinnées par `planet.toml`/`planet-drop.toml`). **797 tests**, 0 warning, capture `planet-challenge` **pinned + verdict visuel humain PASS** (`ea6ba910…`), les 6 autres scènes `SceneRecipe` re-vérifiées inchangées après 2 re-cooks complets forcés, `--scene planet-challenge` → exit 1 confirmé (D9, nomme `LandingChallenge`), Sandbox + HeadlessSim **JIT == NativeAOT**, 0 leak / 0 validation.

Double audit `csharp-lowlevel` + `engine-architect` PASS-with-concerns, **1 🔴 trouvé indépendamment par les deux — la reprise `AGAPANTHE_LOAD`/F5-quicksave de `planet-challenge` était cassée par cette migration**, et **ce n'était pas propre à 3c-2** : le même mécanisme cassait déjà `planet-drop` depuis 3c-1 (jamais détecté faute de protocole de reprise vérifié humainement pour cette scène-là). L'ancien `PlanetStage.Build` passait `spawnEntities: !loadMode` pour garder le monde vide en mode reprise (`GameWorld.Load` refuse un monde peuplé) ; `SceneRecipe`/`SceneMaterializer` peuplaient toujours la scène sans condition. **Décision présentée à l'humain** (le correctif touche `SceneMaterializer`/`SceneLoader`/`SceneRecipe`, donc toute scène `SceneRecipe`, pas seulement celle-ci) — **option (a) choisie : corriger maintenant** (sous-vague BW-015b). `SceneMaterializer.Materialize` gagne `bool spawnEntities = true` (les deux surcharges) : à `false`, tous les modèles référencés restent chargés (le client doit les avoir uploadés pour que `GameWorld.Load` résolve un `AssetRef` restauré), `PhysicsSystem` reste attaché, mais aucune entité n'est spawnée. `SceneRecipe.Build` calcule `spawnEntities = sim.Options.LoadPath is not { Length: > 0 }` **avant** de matérialiser, et — si `AGAPANTHE_LOAD` est posé — restaure depuis ce chemin runtime en priorité sur tout bloc `[restore]` cuit (qu'aucune scène n'utilise de toute façon — un chemin fixe au cook ne pourrait jamais correspondre à un override runtime). **Vérifié bout-en-bout en live** (pas juste par test unitaire) : `AGAPANTHE_SAVE` sur `planet-challenge` (3 entités) → relance `AGAPANTHE_LOAD` → « 0 entities from cooked data » puis « world restored … 3 entities », 0 leak — même round-trip reproduit sur `planet-drop` (prouvant le même bug latent corrigé) et sur le binaire NativeAOT. Les 7 captures pinnées restent inchangées (le correctif ne s'active que si `AGAPANTHE_LOAD` est posé). 6 autres findings 🟠 appliqués : garde d'attracteur de la fabrique vérifiait `Physics is null` au lieu de `Mu > 0` (même forme de bug que le 🔴 de 3c-1, jamais déclenché faute de scène `mu=0` + `landing_challenge`) · `SceneCompiler` ne validait que `AttractorMu > 0`, pas `AttractorSurfaceRadius > 0` · aucune validation numérique sur les nouveaux champs (`target_count`/`shot_budget` négatifs atteignaient un `OverflowException` brut ; NaN rendait le défi silencieusement ingagnable) — 5 nouveaux tests · `QuicksavePath` non validé (chemin absolu/`..` maintenant rejeté au cook, cohérent avec la posture Contenu-2 sur les chemins embarqués) · `CookerVersion` pas bumpé pour le saut v2→v3 (filet de sécurité incrémental désarmé) · commentaire périmé sur `AgSceneFormat`. **Dette laissée** (→ 3c-3) : le champ commun `ProbeModel`/`ProbeRadius` de `SceneSystem`, `required`, forcera un futur système non-spawneur à fabriquer un faux modèle probe · `world_origin` appliqué aux entités mais pas à `AttractorCenter`/`Centre`/`ZoneCenter` (inerte tant que l'origine est zéro partout). **Verdict visuel humain : PASS** (2026-09-12).

**Contenu-3c-3 (session 37, clos) — `ground_quad` + migration `drive` + nettoyage final — Contenu-3c CLOS (3/3), domaine Contenu entièrement clos** : dernier des **3 sous-phases gatées**. Spec `docs/plans/2026-09-11-content-3c-scene-systems-design.md` §11 (ajoutée cette session). **Après cette phase, toutes les scènes du Sandbox tournent sur `SceneRecipe` cuit — plus aucune recipe hand-codée.** **`.agscene` v3→v4** : `SceneSystemKind.DriveControl` — 1ᵉʳ système qui ne spawne rien, ce qui force un changement structurel (pas juste additif) : `SceneSystem.ProbeModel`/`ProbeLocalMesh`/`ProbeLocalMat`/`ProbeRadius`, `required` depuis 3c-1, deviennent optionnels/par défaut (ferme la dette 🟡 F2 de l'audit 3c-2). Nouveau **`MaterializeResult.SpawnedEntities`** (`IReadOnlyList<EntityRef?>`, parallèle à `SceneDefinition.Entities`) — `SceneMaterializer.Materialize` capture désormais la référence que `SpawnBody` renvoyait et jetait avant. **Nouvelle capacité d'authoring découverte en cours d'implémentation** : `[[entity]]` n'avait aucun support de corps physique (seul `[[cluster]]` en construisait) — ajout de `body`/`velocity` en réutilisant les champs `InverseMass`/`Restitution`/`Radius` déjà présents pour `[[cluster]]`. Nouveau `GroundQuadGenerator` (cook-time, `ModelContent.BuildGroundModel`/`BuildGrassImage` déplacés verbatim, seed fixe pour capture reproductible). Nouveau `DriveControlSystemFactory` (client) câble exactement l'`InputMap`/`SampleInput`/`ApplyCommand` que `DriveSceneRecipe` avait à la main, renvoie un `ISystem` no-op (drive n'a jamais eu besoin d'un système par-tick). **Garde single-slot de `SceneRecipe` élargie** à `InputMap`/`SampleInput` (ferme la dette 🟡 F4 de 3c-2). Supprimés (0-appelant grep-vérifié) : `DriveSceneRecipe.cs`, `ModelContent.cs` (entièrement mort), `SandboxCameras.cs` (entièrement mort), `BenchSpinSystem.cs`/`ChurnSystem.cs` (dette pré-existante balayée au passage), `RecipeInput.WireFreeFly` (mort depuis Contenu-3b). `drive` devient `SceneRecipe("drive")` + `content/procedural/ground.toml` + `content/scenes/drive.toml` (`models/DamagedHelmet.glb` fixé — D6, perte de capacité déjà acceptée ; nombres cuits en instrumentant temporairement l'ancien code, jamais committé). **831 tests**, 0 warning, capture `drive` **pinned + verdict visuel humain PASS** (`9030f6a6…`), les 7 autres scènes re-vérifiées inchangées après 2 re-cooks complets forcés, `--scene drive` → exit 1 confirmé (D9, nomme `DriveControl`), Sandbox + HeadlessSim **JIT == NativeAOT**, 0 leak / 0 validation.

Double audit `csharp-lowlevel` + `engine-architect` PASS-with-concerns — cette vague clôturant les 3 sous-phases, l'audit a aussi revu la dette 🟡 accumulée sur l'ensemble de Contenu-3c. **2 🟠 trouvés indépendamment par les deux et corrigés** : `drive_control` + une reprise en attente (`AGAPANTHE_LOAD`/`[restore]`) plantait au démarrage, contredisant le §11 du spec qui promettait un no-op — contrairement au seed paresseux de `LandingChallengeSystem` (qui ne dérive qu'un *compte*, re-dérivable après reprise), `DriveControl` ne peut fondamentalement pas résoudre « le corps à l'index N du cook » après une reprise (le monde restauré n'est pas cette liste ordonnée) → rejeté explicitement au cook-time (`SceneCompiler`) et au runtime pour le cas `AGAPANTHE_LOAD` (`SceneRecipe`), plutôt que de tenter un no-op cassé · `[[entity]] body = true` sur un modèle multi-mesh ou un prefab multi-membres spawnait silencieusement N corps co-localisés qui se pénètrent mutuellement (`Place` timbre le même `SceneBody` sur chaque entité member×mesh) → rejeté au cook-time sauf exactement une entité produite. 5 findings 🟡 supplémentaires appliqués : `body`/`velocity` fuyaient sur `[[grid]]`/`[[cluster]]` et y étaient silencieusement ignorés (violait la garantie de `RejectUnknownKeys`) · modèle-probe `None` sur un système non-`DriveControl` (blob forgé) différait vers un échec tardif et peu clair → rejeté immédiatement · état mutable + abonnement `KeyPressed` vivaient sur la fabrique singleton au lieu de l'instance par-`Create` → déplacés · `FirstOrDefault` silencieux sur fabrique dupliquée → lève désormais · **la seule dette 🟡 accumulée sur les 3 phases jugée digne d'être fermée avant la clôture du domaine (recommandation des deux audits)** : `world_origin` appliqué aux entités mais pas à `AttractorCenter`/`Centre`/`ZoneCenter` → rejeté au cook-time plutôt que laissé comme mine silencieuse. `CookerVersion` bumpé à `contenu3c-3`. **Dette laissée** (hors scope, backlog) : environnement procédural en vrai `.agenv` (Contenu-2b) · nom d'entité→`GlobalId` (D8, aucun consommateur) · validation `ProbeRadius`/`Every` · scripts de dérivation non committés. **Verdict visuel humain : PASS** (2026-09-12).

**Slice-2 (session 38, clos) — 2ᵉ slice dissemblable : `samples/TopDown`, caméra orthographique réelle, `Agapanthe.Platform.App` partagé** : le test de généralité le moins cher du backlog §4quater. Spec `docs/plans/2026-09-12-slice2-topdown-design.md`, approuvée **4,3/5** (revue scorée séparée, 8/8 affirmations factuelles vérifiées, 1 vrai écart de Consistency corrigé). Une **2ᵉ app séparée** (`samples/TopDown`, pas une scène du Sandbox) avec une caméra orthographique fixe overhead sur un diorama plat, pour révéler ce qui était silencieusement câblé en dur pour la forme planétaire/perspective de VS-1/2/3 et tout Contenu-3c. **`Camera` gagne une vraie projection orthographique** (`CameraProjection.Perspective`/`Orthographic`, nouveau `MathHelpers.OrthographicVulkanReversed` — dérivation reversed-Z distincte de la perspective : `w=1` constant en orthographique contre `w=z_view` en perspective, d'où `M33=-M33; M43=1-M43` au lieu de `M33=-1-M33; M43=-M43` ; la 1ʳᵉ tentative a copié la formule perspective, un test TDD l'a attrapée immédiatement). **`.agscene` v4→v5** : `SceneCamera.Fixed` gagne `Projection`/`OrthoWidth`/`OrthoHeight`. **`EngineWindowAdapter` + `DriveControlSystemFactory` extraits** de Sandbox-`internal` vers un nouveau projet partagé **`src/Agapanthe.Platform.App`** (réf. `Platform`+`App`) — Sandbox et TopDown référencent désormais les mêmes classes, zéro duplication (vérifié par un déplacement pur : les 8 captures Sandbox restent byte-identiques). Ombres CSM explicitement hors scope (D3 — `ShadowFit` suppose un cône perspective, faux pour une boîte orthographique) : `topdown.toml` a `casts_shadow=false` sur ses 4 entités. Caméra fixe overhead, réutilise `DriveControlSystemFactory` tel quel (pas de nouveau système caméra-suit-body, D4). MSBuild : `TopDown.csproj` duplique les targets shader-precompile/font-cook du Sandbox plutôt que de les extraire davantage (D6, décision assumée). **2 vrais bugs de généralité trouvés en testant live, pas en relisant le code** : `SceneRecipe.cs` (code partagé `Agapanthe.App`) avait `"Sandbox: "` codé en dur dans 2 lignes de log — corrigé en `"AppHost: "` ; le puck de `topdown.toml` n'avait pas `casts_shadow=false` explicite (défaut `true`, violait D3 silencieusement) — corrigé. **854 tests**, 0 warning, capture `topdown` pinned + **verdict visuel humain PASS** (`31ea748d…` — 3 sphères de même rayon rendent en cercles de même taille apparente quelle que soit leur profondeur x/z, preuve qu'il n'y a aucun raccourcissement en perspective), les 8 captures Sandbox re-vérifiées inchangées, `HeadlessSim --scene topdown` → exit 1 confirmé (D9, nomme `DriveControl`), les 3 binaires **JIT == NativeAOT** sur toute capture/snapshot (re-vérifié après les correctifs d'audit), 0 leak / 0 validation partout.

Double audit `csharp-lowlevel` **4,3/5** + `engine-architect` **4,2/5** PASS-with-concerns, **aucun 🔴** — les deux ont vérifié indépendamment et symboliquement la dérivation reversed-Z orthographique (correcte) et le déplacement `Agapanthe.Platform.App` (byte-identique, diff `HEAD` ne montre que namespace/visibilité). **4 findings trouvés par les deux et corrigés** : `projection`/`ortho_*` silencieusement ignorés sur une caméra `frame-bounds` (même classe de bug que `body`/`velocity` fuyant sur `[[grid]]`/`[[cluster]]`, fermée en 3c-3) — `SceneCompiler.BuildFrameBoundsCamera` rejette désormais toute valeur non-défaut · `Agapanthe.Platform.App` sans entrée d'allowlist statique (`EngineIsHeadlessTests`) — ajoutée, patron de chaque projet structurant depuis MP-0a · 2 commentaires périmés nommant le `samples/Sandbox/EngineWindowAdapter` supprimé — corrigés · l'orthographique n'a aucune correction d'aspect ratio automatique (la perspective l'a gratuitement via `FovY`+`AspectRatio`) — **`Camera.OrthoHeight=0` devient la sentinelle « dérive de `OrthoWidth`/`AspectRatio` »** (convention `MoveSpeed`/`ShadowDistance`), le hack manuel `ortho_height=22.5` du TOML remplacé par la sentinelle `0.0` (re-capture byte-identique, la dérivation reproduit exactement la même valeur). **1 finding csharp-lowlevel supplémentaire** : `OrthoWidth`/`OrthoHeight` validés au cook seulement, pas au read `.agscene` ni dans `Camera` — un blob forgé ou un `new Camera` nu pouvait produire une matrice NaN/Inf sans message de validation ; gardes symétriques ajoutées dans `AgSceneFormat` et `Camera.ResolveOrthoHeight()`. **Dette laissée** (assumée, versée au backlog §4quater) : le garde-fou D3 (ombres) ne repose que sur une convention TOML sans filet de code · `DriveControl` est maintenant 2 scènes/2 apps refusées par `HeadlessSim` — signal de conception pour le netcode (un corps pilotable est le cas d'usage canonique d'un serveur autoritaire), pas un bug · exposition HDR câblée en dur pour le studio HDRI du Sandbox, aucun champ `.agscene` · `skybox.vert`/`FreeCameraController` faux en orthographique (mineur, non exercé) · coût de build ×3 (chaque app cuit tout `content/` dans son propre `obj/`) · garde `Frustum.Normalize` `1e-8` dégrade plus tôt à l'échelle planétaire orthographique (requalifie la dette pré-existante, ne l'ajoute pas). **Verdict visuel humain : PASS** (2026-09-12).

**Prochain (session 30+) — cap « vrai engine »** (backlog **§4quater**). La Vertical Slice a prouvé l'intégration : **VS-1 sérialisation** (S22), **VS-2 spawn runtime + gravité newtonienne** (S23), **VS-3 glu gameplay** (S24) — toutes CLOSES. **VS-4 (HUD) et VS-5 (audio) sont EN PAUSE** (décision humaine S25). Nouveaux ancrages : **l'artefact = le moteur** (pas un jeu) · généraliste **mais** spécialement sims spatiales grande échelle, un Stardew-like doit rester faisable · **multijoueur pensé dès maintenant, serveur autoritaire** (PAS lockstep : byte-identité intra-binaire seulement) · **massif/persistant visé, petite coop possible** → **la topologie est un choix de DÉPLOIEMENT, jamais d'architecture** (même code sim partout, seule l'autorité change ; ownership = concept dès le départ). MP-0 (fondations d'autorité) est **décomposé en 4 sous-jalons** : **MP-0a headless split** ✅ (S26) · **MP-0b identité** ✅ (S27, `GlobalId` 64 bits partitionné + `ContactPairKey`) · **MP-0c autorité du temps** ✅ (S28, fixed-timestep accumulator) · **MP-0d input → commandes horodatées** ✅ (S29, `SimCommand`/`InputSnapshot` blittables + `SimCommandQueue` + `InputMap` déclaratif + phase dans `SimulationHost.Tick` + `GameWorld.SetBodyVelocity` + `HeadlessSim --drive`). **MP-0 CLOS (4/4).** **`Agapanthe.App` CLOS (S30)** — `AppHost` + contrat `IGame`/`ISceneRecipe`. **Domaine Contenu ENTIÈREMENT CLOS** : identité d'assets stable → cook offline + manifest → `AssetRef` sim-side → format `.agscene`/`.agprefab` cuit + `SceneLoader` (client et serveur partagent enfin le peuplement) → **Contenu-3c CLOS (3/3, S35-S37)** : registres de fabriques + migration `planet`/`planet-drop` (3c-1) → `LandingChallenge` + `planet-challenge` + correctif générique de reprise (3c-2) → `ground_quad` + migration `drive` + nettoyage final (3c-3). **Toutes les scènes du Sandbox sont désormais des données cuites, zéro recipe hand-codée.** **Slice-2 CLOS (S38)** — `samples/TopDown`, caméra orthographique réelle, `Agapanthe.Platform.App` partagé (solde la dette `EngineWindowAdapter`) ; a confirmé que la scission `SimSceneContext`/`PresentationSceneContext` (Contenu-3a) tient sans modification pour une 2ᵉ app. **UI-3 CLOS (S39)** — timestamps GPU + seam `FrameProfiler` fermé par découplage ; **domaine Texte & UI ENTIÈREMENT CLOS (3/3)**. **Queries physiques — raycast + layer mask CLOS (S40)** — `GameWorld.TryRaycast`/`RaycastAll` contre tout drawable (`Bounds`, pas seulement `RigidBody`), `QueryLayer` optionnel (masque, authoring TOML `layer`, `.agscene` v6), `Camera.ScreenPointToRay` (perspective **et** orthographique — un premier jet perspective-only a été trouvé et corrigé par le double audit), démo `Key.F` (crosshair écran-centre, Sandbox + TopDown). Double audit `csharp-lowlevel` (3,6/5, **1 🔴 trouvé-et-corrigé** — `maxDistance=+Infinity` bloquait le raycast en boucle infinie) + `engine-architect` (4,1/5), tous deux PASS-with-concerns, forte convergence (3 findings trouvés indépendamment par les deux). Prochain : un autre item §4quater — audio, queries de formes, job system → netcode. Détail + point de reprise dans `docs/AVANCEMENT.md` § Reprise.

**P3-M1 (session 14, clos)** : instancing via SSBO (draw calls **12 556 → 2** à 10k, batching par (matériau, mesh), `firstInstance`) + les **2 dettes de culling de M4 soldées** (`AggregateBounds` par frame, `ExtrudedShadowFrustum` ANDé au volume de lumière). Double audit PASS. 284 tests, 0 alloc/frame, AOT PASS.

**P3-M2 (session 15, clos)** : nouveau projet **`Agapanthe.Engine`** (marie World + Rendering, ne référence pas Platform, ne possède rien). L'**ordre de frame quitte le Sandbox** → `FrameOrchestrator` + `SystemScheduler` à étages (`Input→Simulation→PostSimulation→Render`), `Tick` hors `DrawFrame` (D1.a). **Lifecycle** : `Spawn`/`Despawn`/`SetParent`/`IsAlive` différés à une barrière, **`Despawn` cascade**, `EntityRef` = `GlobalId` (file de commandes propre au World, pas le `CommandBuffer` d'Arch). **Cull d'ombre deux passes** : wedge borné en amont (7ᵉ plan, `ShadowCasterDistance`), `UpstreamExtent` depuis les **casters** (plus les bounds globales). Double audit PASS, findings appliqués (garde F7 `_pass1ShadowList`). Banc `grid:100x100` Release+**AOT** : draws 2+2, 0 alloc/frame, 0 leak, **311 tests**, capture bit-identique `9790D95D`. Commit `90627a5`. Env var : `AGAPANTHE_CHURN=N` (stress lifecycle).

**P3-M3 → M8 (sessions 16-21, tous clos, double audit + verdict visuel PASS chacun)** : **M3** physique v1 (corps rigides linéaires déterministes, gravité, sphère↔sol/sphère, broadphase grille 0-alloc) · **M4** rendu GPU-driven (`scene_cull.comp` frustum-cull + compaction atomics, `vkCmdDrawIndexedIndirect`, offset de batch en push constant → MoltenVK-safe ; GPU visible == CPU) · **M5** CSM (4 cascades, atlas 2×2, `castsShadow` par entité) · **M6** slots persistants dirty-trackés (`PersistentInstanceBuffer`, gather+radix sort au rebuild structurel seulement, frame ordinaire O(dirty)) + cull d'ombre GPU (`shadow_cull.comp`) · **M7** buffers device-local (`CommandList.CopyBuffer` async, core `vkCmdCopyBuffer`) + raster d'ombre 4×→1× (7ᵉ plan near-cut, A+B ~15,3 → ~8,0 ms) · **M8** premier pas planétaire : **reversed-Z global** (`PerspectiveVulkanReversed`, comparateur depth **par pipeline** → passe d'ombre découplée) + `Primitives.UvSphere` + scène `AGAPANTHE_SCENE=planet` (planète + Soleil sphère emissive à **7,48e10 m**, échelle **1/2 uniforme**, **point light co-localisée avec la sphère-Soleil** = seule lumière, env noir). 333 tests, 0 alloc/frame, AOT PASS. Env vars : `AGAPANTHE_PHYSICS=1`, `AGAPANTHE_CULL_VERIFY=1`, `AGAPANTHE_SUN`, `AGAPANTHE_PLANET_{RADIUS,PHASE,ALT,FOV}`, `AGAPANTHE_SUN_{DIR,RADIUS,DISTANCE}`.

**VS-1 (session 22, clos) — sérialisation du World** : `GameWorld.Save/Load(Stream)`, snapshot **binaire blittable déterministe** (header `AGWD` + entités triées par GlobalId + `presenceMask` u32 sur l'ordre `ComponentRegistry.All` ; `InstanceSlot` exclu, `Parent`→GlobalId). GPU-free (aucun type Arch ne fuit), dispatch 3-switches concrets = AOT-safe, **pas de générateur** (blittable). Load sur World frais (compteur restauré sans bump, remap `Parent` par GlobalId, archétypes reconstruits fidèles). Robustesse : magic/version/count/tronqué/mask-hors-plage/GlobalId-dupliqué → `WorldSerializationException`. Seam GPU = **handles reproductibles** (Option 1, recharger les mêmes assets d'abord). Sandbox `AGAPANTHE_SAVE`/`AGAPANTHE_LOAD` (round-trip cross-process byte-identique JIT+AOT). Double audit PASS. **346 tests**, garde d'ordre `ComponentRegistry.All` figée + garde ≤32 composants.

**VS-2 (session 23, clos) — spawn runtime différé + gravité newtonienne** : `SpawnBodyDeferred` (jumeau runtime de `SpawnBody`, `CommandKind.SpawnBody` + 4 champs plats sur `StructuralCommand`, appliqué à la barrière structurelle, `MaterialiseBody` = point de matérialisation unique immédiat/différé) solde la dette P3-M3 (spawn de corps en cours de simulation sans muter les archétypes sous l'itération de `StepPhysics`). **Gravité newtonienne** minimale (décision humaine en interview) : attracteur unique **dans `PhysicsSettings`** (`WithAttractor(C, μ, R)`, pas un composant → ordre `ComponentRegistry.All` et masque VS-1 intouchés) ; `StepPhysics` branche sur `Mu>0` — gravité radiale inverse-carré `a=−μ·(p−C)/|p−C|³` (garde singularité `r2>1e-18`) + **sol radial** `|p−C|−r<R` (rest-clamp `2·(μ/R²)·dt`, PAS `gravity.Y`). **`μ=0` → chemin uniforme byte-identique** (scène `drop` protégée). Pas d'orbites (Euler+velocity `float`), pas de n-body. Sandbox `AGAPANTHE_SCENE=planet-drop` + `ProbeDropSystem` (Stage.Input) + keypress `B` ; profils Rider dans `Sandbox/Properties/launchSettings.json`. 355 tests, 0 alloc hot path, AOT PASS (`iterated 13`), capture headless déterministe. Double audit PASS-with-concerns (les 2 ont trouvé le **même 🟠** singularité `r2≈0`, corrigé) + verdict visuel PASS.

**VS-3 (session 24, clos) — glu gameplay : défi d'atterrissage planétaire** : câble **input → spawn (VS-2) → règle d'état → save/resume (VS-1)** sans nouveau chemin de rendu. Trois couches : le World gagne une **requête spatiale générique** `QuerySurfaceContacts(C,R,band,zoneC,zoneR) → LandingCounts(Total,Airborne,InZone)` (itère `BodyDesc`, 0-alloc prouvé par test, aucun type Arch ne fuit, « posé=SUR la surface » sans seuil de vitesse) ; la **règle pure latchée** `LandingChallengeRule.Evaluate(counts, shotsIssued, prev)` dans `Agapanthe.Engine` (Won si `InZone≥N`, Lost si budget épuisé **et** `Airborne==0`, terminal monotone intra-session) ; le `LandingChallengeSystem` (PostSim, Camera + `window.Title`) dans le Sandbox. Boucle `AGAPANTHE_SCENE=planet-challenge` : `B` largue une probe **radiale sous la caméra** (chute newtonienne VS-2), poser **N=3** en **≤ M=6** tirs ; `TryShoot` gardé par `_shotsIssued<M` (pas la query) ; titre reconstruit seulement sur changement `(InZone,shots,status)` → 0-alloc régime stable. Beacon émissif = drawable (pas un body). **Save/resume relaunch-only** (`F5`→Save flush intégré ; `AGAPANTHE_LOAD`) : **zéro octet ajouté au snapshot VS-1** — `_shotsIssued` re-semé du body count post-Load, statut re-évalué ; constantes de scène épinglées dans les profils Rider `planet-challenge`. 372 tests (17 GPU-free), 0 warning, headless 0 validation/0 leak (226 resources), AOT PASS. Double audit PASS / PASS-with-concerns 4/5 + verdict humain PASS.

**Dette persistante** : ✅ ~~`CurrentTick.FrameIndex` post-incrément~~ **résolu MP-0c** (`FrameIndex`→`TickIndex`, `CurrentTick` = `Math.Max(0L, TickIndex-1)` = dernier tick exécuté, épinglé) · ✅ ~~🟠 **seam `FrameProfiler` reporté à UI-3** (UI-2)~~ **résolu UI-3** (S39, découplage total — `FrameStats`/`FrameSeries` intouchés, la série GPU vit côté `Renderer`/`DebugOverlaySystem`) · 🔴 **Linux jamais validé** (premier item P3, non bloquant pour la Vertical Slice) · 🟠 **latch Won/Lost VS-3 non-monotone à travers un reload** (`_status` re-dérivé du monde, zéro octet au snapshot ; un probe glissant hors/dans la zone entre save et reload peut un-win/un-lose — faible proba ; alternative `.state` 1 octet déférée) · 🟠 **CSM `_casterSpheres` dans le World** (F7, une `RenderView`/frame — gardée) · 🟠 **`UpstreamExtent` par cascade** déféré (near-cut calé sur épaisseur de tranche, pas longueur d'ombre — soleil très rasant = cas limite ; backlog §2.0bis) · 🟠 **pas de lifetime/rest-cull des corps runtime** (VS-2 m2 : le spawner de la démo fait croître les entités sans borne → politique de durée de vie nécessaire pour l'ancre persistante) · 🟠 cascade despawn O(profondeur×N) quand un despawn est en attente · 🟠 `SortKey` sans profondeur (+ compaction atomique scramble l'ordre → 2ᵉ verrou transparence) · 🟡 garde normalisation `Frustum` `1e-8` (near sub-mètre) · plafonds **16 bits** mesh/matériau et **`GlobalId < 2³²`** (clé de contact physique) à faire échouer bruyamment · ✅ ~~**sérialisation (VS-1) : `Generation` des handles non validée au load** → un ordre de chargement d'assets différent casse en silence~~ **résolu Contenu-1** pour mesh + material (les seuls handles que `MeshRef` porte) : le snapshot v3 stocke une `AssetKey` + indices locaux, re-résolus au load, un ordre de chargement différent est désormais soit correct soit une `GraphicsException` bruyante. Reste hors scheme : textures / environnement / fonts (pas portées par le snapshot aujourd'hui) · ✅ ~~identité d'asset **côté simulation**~~ **résolu Contenu-3a** (composant `AssetRef` #13 ; un `Save` headless émet de vraies `AssetKey`) · device-local/transfer **non exécuté sur MoltenVK** (P3-M0) · crash shutdown GLFW/Silk.NET (~1 run/5, **après** le rapport de leak propre — le gate CI est keyé sur la ligne de rapport, pas l'exit code) · ✅ ~~🟠 **`SceneContext` indissociablement client**~~ **résolu Contenu-3a** (`SimSceneContext` headless-safe + `PresentationSceneContext` nullable) puis **partiellement résolu Contenu-3b** : `SceneMaterializer`/`SceneLoader` sont désormais le code de peuplement unique client+serveur — reste **le câblage `MaterializeResult.Physics`/`RestorePath` → `PhysicsSystem`/`RequestRestore` dupliqué par hôte** (`Engine` ne peut nommer `MaterializeResult`, `App` porte Vulkan), forme concrète de la décision `App`/`App.Client` à venir · ✅ ~~🟠 `EngineWindowAdapter` (~75 l de forwarding) à recopier par toute 2ᵉ app~~ **résolu Slice-2** (`src/Agapanthe.Platform.App`, partagé Sandbox+TopDown, `DriveControlSystemFactory` y migre aussi) · 🟠 helpers caméra/lumière/sol/sky **partiellement remontés dans `App`** (Contenu-3b : `SceneCameraApplier`/`SceneLightRig`/`SceneInput`) ; ceux de `drive`/`planet*` restent dans le Sandbox jusqu'à leur migration 3c · 🟠 `Agapanthe.App` → `App` + `App.Client` (Vulkan est dans la closure d'App) avant un vrai `RunDedicatedServer` · 🟠 coût GC de mark du `AssetRef[]` managé non mesuré (Contenu-3a — fallback D1-alt documenté) · 🟠 **`ResourceRegistry.Unload` a 0 appelant** depuis la suppression de `ModelSceneRecipe` (Contenu-3b) — a besoin d'un chemin d'exercice GPU réel (idéalement le reload de scène de 3c) pour re-garder le leak de descripteurs par matériau que l'historique `AGAPANTHE_UNLOAD_TEST` fermait · 🟠 **le garde-fou D3 (ombres CSM hors scope en orthographique) ne repose que sur une convention TOML** (`casts_shadow=false` répété par entité), aucun filet de code ne détecterait une scène orthographique qui l'oublie (Slice-2, audit `engine-architect`) · 🟠 **`SceneSystemKind.DriveControl` est maintenant 2 scènes/2 apps refusées par `HeadlessSim`** — signal de conception pour le netcode, pas un bug : un corps pilotable est le cas d'usage canonique d'un serveur autoritaire, et le refus blanket sur `Systems.Count` ne distingue pas *le système* (client) de *la déclaration de scène* (sim-side) (Slice-2, audit `engine-architect`) · 🟡 exposition HDR câblée en dur (`AppHost.cs`, calée sur le studio HDRI du Sandbox), aucun champ `.agscene` ne permet à une scène d'en authorer une · 🟡 `skybox.vert`/`FreeCameraController` faux en orthographique (non exercé aujourd'hui) · 🟡 coût de build ×3 par app (chaque app cuit tout `content/` dans son propre `obj/assetcache`) · 🟠 **UI-3 : 4 risques MoltenVK documentés pour P3-M0** (`stage` de `vkCmdWriteTimestamp2` ignoré, granularité per-encodeur-Metal sur Apple Silicon, fallback CPU à zéros francs, 2 bugs MoltenVK ouverts sur le patron reset+lecture double-buffered — détail `docs/BACKLOG.md`) · 🟡 UI-3 : le chemin matériel-réellement-non-supporté n'a aucune couverture de test (seul le chemin explicitement-désactivé `AGAPANTHE_GPU_TIMESTAMPS=0` est exerçable sur cette machine) · 🟡 UI-3 : le `Total` GPU affiché ne couvre pas la sync de la copie candidats ni les transitions swapchain (hors des 4 régions instrumentées, documenté dans le spec).
